# 63-U2 学校のあだ名禁止：7/26保存回の未採用31件を整理

SNS反応まっぷの課題63に関する、テーマ別の修復指示文です。このテーマだけを担当し、証拠付きの修正候補と検査結果まで仕上げてください。

- 対象テーマ: `school-nickname-ban`
- 正典: `social-samples/school-nickname-ban_hermes_arena_classified.json`（着手時はTHEMES.yamlのsample_fileを正とする）
- 関連課題: 63 / 40
- 作業状態: 指示文準備済み。修復は未着手。

## このセッションの進め方

1. プロジェクトの `CLAUDE.md` に従い、開始時の必読文書と `DATA_REFRESH.md`、開発・データ部の文書を読む。テーマ記録は担当テーマの1本を読む。
2. 最新main、専用worktree一覧、同テーマの作業ブランチを確認する。同じテーマを修復中のセッションがあれば成果・作業範囲を確認し、二重修復を始めない。他セッションを停止したり、勝手にブランチを変更したりしない。
3. 専用worktree（作業用コピー）を最新mainから作る。原本は `OPERATIONS.md` に従って復元し、共有側の最新原本とも件数とSHA-256（内容の指紋）を照合する。共有コピーが新しい場合はその差の保全・由来を確認してから使用する。
4. 課題63案は `task/x-data-audit-20260906` のコミット `1c383d5` にある。mainに未反映なら `git show` で `tasks/task-63.md` と `quality/reviews/2026-09-06-x-data-audit.md` を読む。提案ブランチ全体を無条件に取り込まない。
5. 下の数字は2026-09-06、main `3887c98` の観測値。着手時に投稿IDで再計算する。数字が変わっていたら最新の差分を対象にし、当初値と増減理由を記録する。

## 監査時点の状態

- 現行正典432件、意見93件。取得日時欠損0件。
- `social-samples/school-nickname-ban_hermes_cur_20260726.json` の45件のうち、31件が現行正典にない（14件は既に存在）。
- 課題40の「残45件」は固定の過去記録であり、再計算が必要。
- テーマ記録では無関係な投稿とニュース共有が多いことが確認されている。意見が少ないこと自体を異常としない。

## 正典に入っていない投稿を扱う手順

- 対象保存回のraw・分類済みファイル・report・作業ログ・Git履歴を照合し、現行正典との差を投稿IDで取り直す。
- 各投稿を「既に採用済み／採用漏れと確認／意図的な保留・除外／分類変更が必要／理由不明」に分け、根拠を残す。正典外というだけでは採用漏れと断定しない。
- 正典外の投稿は本文を読み、対象テーマとの関係、意見か情報共有か、現行の論点・賛否との適合を確認する。現在の分類器の宣言だけで過去分類の意味が同じと決めつけない。
- 採用漏れが確認できた分は、元の保存回を残したまま累積の修正候補へ統合する。理由不明・既知の保留は別一覧に残す。論点体系が違う場合は必要な投稿だけの再分類候補を作り、モデル・分類基準・入力の指紋を記録する。共有のAIモデル設定は変更しない。
- 追加前後の収集数、関連数、意見数、論点・賛否分布を比較する。既存投稿を消さず、追加IDが保存回に由来し、意図しない重複がないことを検査する。
- 追加で古くなる主張照合・再読記録・取得期間・潮目比較を洗い出す。旧回の追加を「今日の新着」と扱わない。対象の本文確認が必要なら差分作業の一覧を残す。
- 完了条件: 現在の正典外IDすべてについて処置と根拠を記録し、採用漏れ確定分は検査済み候補へ反映する。理由不明は保留と明示し、全件統合したかのように報告しない。

## このテーマで追加確認すること

- 旧回45件を丸ごと追加せず、既存IDと照合する。現在の正典外31件も、理由確認後に処置する。
- 関係するが意見ではない投稿を、意見数を増やすために意見扱いへ変えない。対象外・情報共有の判定根拠を残す。
- 課題40のうち、このテーマの残件数と処置だけの更新案を報告書へ書く。他テーマの完了状態は変更しない。

## 保存・検査・引継ぎ

- 調査だけで終わらず、証拠を確認できた分は修正済み候補に反映し、理由が確定しない分は保留として分ける。推測で埋めて完了にしない。
- 作業開始時の原本、証拠一覧、修正候補、投稿ごとの処置と理由、検査結果を残す。本文や投稿IDを含む候補は `.staging/data-repair/school-nickname-ban/<run-id>/` に作成し、Gitには追加しない。
- worktreeを片付ける前に、その非公開成果一式を `/Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/school-nickname-ban/<run-id>/` へ保管する。元ファイルと保存先のハッシュ一致、および保存先から候補を復元できることを検査する。`.staging/` は既存の自動バックアップ対象ではないため、保存したつもりで片付けない。既存の保存回を上書きしない。
- 本文・IDを含めない報告を `quality/reviews/<作業日>-data-repair-school-nickname-ban.md` へ作る。再実行に必要なコマンド、参照データの指紋、非公開成果の保存先、修正前後の件数を残す。
- 候補の投稿IDが一意で、本文・既存の分類・再読成果を意図せず変更していないことを検査する。分類変更や投稿追加が必要な場合は、その差分を別に数えて説明する。
- 候補へ差し替えた作業環境で、変更に関係する既存検査を実行する。取得期間なら `verify_sample_periods.py`、集計なら対象テーマの `verify_theme_page.py` と `verify_number_provenance.py`、新ページの再読に関係する場合は最新の再読検査も確認する。失敗は原因を切り分け、検査を緩めて通さない。新ページ設定が未作成なら、その検査は未実施と理由を明記する。
- 他テーマのデータ、共通分類器、全体のモデル設定、公開HTMLは担当外。共通処理の不具合は最小再現例を報告し、今回の修復候補と切り分ける。
- テーマ別の作業記録へ「候補作成済み／未適用」と証拠を記録する。課題索引・課題63の共通ファイルは複数セッションで同時編集せず、対象小項目の状態更新案を報告書に添え、統合担当へ渡す。現在の収集予定日・公開更新日を、修復の作業日で進めない。
- mainへの取り込み、共有側の正典置換、一般公開はこのセッションの最終報告後に扱う。公開候補を作る場合は `DATA_REFRESH.md` の手順を守る。全テーマの生成物を個別セッションで順番に上書きしない（課題59）。
- コードや報告書は専用ブランチへコミットする。非公開成果の復元確認と保存先を残してから、使い終わったworktreeを片付ける。

## オーナーへの最終報告

箇条書きで「何件をどう直したか／何件が保留でなぜか／検査結果／成果の保存先／公開前に残ること」を報告する。原本の修復、公開向け集計への採用、一般公開を混同しない。
