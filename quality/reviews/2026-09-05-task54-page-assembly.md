# 課題54: 山なみを公開ページの部品ごと組み立てた（差し替え見本）

2026-09-05 ／ オーナー指示「部活地域移行のページを、現在のwebページと差し替えて良いくらい
見やすい状態にしたい」への着手。

## やったこと

`scripts/build_planet_page_preview.py` を新設した。公開ページ
`docs/bukatsu-chiiki-reaction-map.html` を入力にして、山なみが役目を引き継ぐ区間を外し、
山なみブロックを差し込んだ見本 `quality/prototypes/bukatsu-chiiki-page-preview.html` を作る。

**docs/ には書かない。**同じ区間を作っている既存の生成器と取り合いになるため（下の「残る仕事」3）。
同じ入力で2回実行して差分0（課題34）。

### 外した区間（5つ）と、取り残されたスクリプト2本

| 外したもの | 山なみ側の受け皿 |
|---|---|
| 7つの論点とXの声（2Dアリーナ） | 山なみ本体 |
| このテーマを読み解く論点（論点カード6枚）＋拡大表示 | 山を押したときの論点パネル |
| 30秒でわかる、両側の強い論拠 | 編集部の横断整理 |
| SNS反応マップ（2Dキャンバス） | 山なみ本体 |

外した部品にしか触っていない素のスクリプト2本（論点カードの拡大、2Dマップの描画）は、
残すと null 参照でJSエラーになるので一緒に落とした。呼び出し側はすべて `if(window.…)` で
守られているため、**投票は動く**（`setStanceMapVoteMarker` の呼び出し3か所とも guard 付き）。

### 残した部品（触っていない）

ヘッダー・フッター・パンくず・投票・Xシェア・関連テーマ・SNS投稿サンプル・
このページの作り方・この争点の背景・世論の潮目・詳細データ・広告枠・OGP・canonical・JSON-LD。
（計測タグだけは、見本を開いても実サイトの数字に混ざらないよう外してある）

### 直した3点

- 試作のCSSが公開ページへ漏れないよう、全セレクタを `#planet-block` の中に閉じ込めた
  （`.panel` と `.lead` が topic-modern.css と衝突する）。
  セレクタ直前のコメントを外し忘れると `.planet-live #fallback` の前置きが壊れ、
  **JSが動いているのに静的表示（7,295px）が出たままになる**。実際に踏んで直した
- 「この試作の見かた」→「このページの見かた」
- 「3Dが使えなくても」→「図が出ないときも」（球は廃止済みで、3Dはもう嘘）
- `#meta` の末尾にあった開発者向けの一文（スクリプト名とコマンドの案内）を落とした

## 分岐元が古かった

作業ツリーが main より7コミット古く、社外に出ない正典（`social-samples/`）を共有ツリーから
同期したところ検査5本が落ちた。main を取り込み、`build_public_registry.py --all` で
公開データを作り直して **471テスト OK**。
**`task/planet-engagement` 自体はまだ7件古い。**次にあの枝で作業する人は先に main を取り込むこと。

## 残る仕事（差し替えの前に必要）

1. **独自性の検査に通らない。**`build_planet_data.py` の検査が
   「『教員の働き方』の未読が236件（全323件の4割超）」で止まる。
   4割は129件なので、**あと約107件を人が本文で読み直す**必要がある。
   これはコードでは片づかない（`--prototype` は検査を迂回するだけで、公開には使えない）
2. **`verify_theme_page.py` が旧ページの形を前提にしている。**組み立て後のページを
   検査に掛けると5件NGになる。中身が悪いのではなく、検査が
   「論点カード6枚に件数がある」「arguments は論点の後・SNS反応マップの前」を求めており、
   山なみの件数を「管理対象外」と判定するため。**検査側の書き換えが差し替えとセットで要る**
3. **同じ区間を作っている生成器の引退。**`build_bukatsu_arena.py` /
   `build_reaction_map.py` / `update_bukatsu_tide.py` / `inject_tide_widget.py` が
   外した区間を作っている。docs/ を直接書き換えると取り合いになる

## 見た目を直した（オーナー指摘「無理やりくっつけたようになってます」）

ヘッドレスのChromeで実際の画面を撮って確認した。指摘は正しく、原因は2つあった。

1. **試作は単体で開く前提の暗い配色。**公開ページは白いカードが並ぶ明るい面なので、
   黒い板を貼り付けたように見えていた。さらに見出し「議論の山なみ」が、公開ページ側の
   `.panel-title h2`（濃紺の文字）と暗い背景の組み合わせで**ほとんど読めなかった**
2. **図の38%が黒い空白だった。**海面より下は「海の水を抜く」まで暗幕で覆われるが、
   場所は最初から確保されているため、押すまで作りかけに見えていた

直した内容（`LIGHT_SKIN`）:

- 配色・書体をサイト側へ寄せた。**図（山なみ）と点の装置だけ暗いまま残した。**
  データを見せる箱が暗いのは旧ページの2Dマップ（`#0f0e2e`）と同じ扱いなので浮かない
- 潜る前は海面より下を畳み、論点名（海面+17px）まで見せる。「海の水を抜く」で開く。
  `prefers-reduced-motion` では動かさない

## 確認できていないこと

- **スマホの実機。**ヘッドレスのChromeは端末エミュレーションをしないため、375px幅の確認は当てにならない。旧: ブラウザのパネルが閉じており、`innerWidth` が0で描画されないため、
  スクロールした状態の画面を見られていない。構造（見出しの並び・山7つ・静的表示が隠れること・
  JSエラーが出ないこと）は確かめたが、**読みやすさは未確認**
- 実機スマホ、「視差効果を減らす」設定
