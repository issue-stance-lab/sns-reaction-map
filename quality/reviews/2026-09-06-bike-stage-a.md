# 課題63 段階A — 自転車データの整備結果

確認日: 2026-09-06（日本時間）

対象: `bike-blue-ticket` の累積正典468件、課題54の見本 `70f8792`、課題62の最新生成器

## 結論

自転車の公開母数は **468件のまま**です。今回、ニュース共有などを意見から外す再判定はしていません。
直したのは、旧形式の「意見」フラグを最新の再読検査が見落として、別の集合として数えていた点です。

その結果、見本を生成すると未読19件を「後から増えた分」ではなく、再読時点ですでにあった**読み飛ばし**として検出し、公開候補を止めるようになりました。これは公開を安全に止めるための正しい結果です。

## 意見の数え方

| 確認項目 | 修正前 | 修正後 |
|---|---:|---:|
| 正典の投稿数 | 468 | 468 |
| 公開ページで使っている意見数 | 468 | 468 |
| 最新生成器が読むネスト形式の意見フラグ | 0件 | 旧形式の468件を明示的に読む |
| 生成器が数える未読内訳 | 0件となり合計と不一致 | 読み飛ばし19件、後日追加0件 |

正典には従来から、全468件にトップ階層の `is_opinion: true` が付いています。これは自転車テーマ固有の従来の母数の決め方で、他テーマの `classification.is_opinion` とは保存場所が違いました。

今回の修正は、トップ階層の既存判定を読み取る互換処理です。新しいフラグを全件に追加して検査を通すことはしていません。意見に含める範囲を狭めるには、本文を読んだ別の編集判断が必要です。その場合は、468件から何件・どの論点が変わるかを比較してから提案します。

## 取得履歴116件

本文を含まない検証サマリ [bike-blue-ticket-fetch-history-recovery.json](../../data/verification/bike-blue-ticket-fetch-history-recovery.json) を追加しました。

- 本文が一致する過去の観測記録を確認できた: 115件
- 投稿IDだけ一致し、本文一致を確認できない候補: 1件
- 過去の候補が見つからないもの: 0件
- 複数の観測日時が残るもの: 52件

複数日の候補を1日に上書きしていません。正典の `fetched_at` / `query` / `source` は変更せず、候補ごとに観測日時・照合元ファイルの指紋・本文指紋を記録しました。本文、投稿ID、URLはこの検証サマリに含めていません。

正典468件全体と、取得日時が欠ける116件のID集合にはそれぞれ指紋を付けました。したがって、候補作成の前後で投稿が増減していないことを本文やIDを公開せずに確認できます。

## 他AIの成果との関係

- `task/bike-rollout` の `70f8792` は見本・追加26件の再読成果として確認しました。今回取り込まず、再読もし直していません。
- `task/bukatsu-reread-stage9a` の `c12c50b` は部活動の成果なので、対象外として重複作業をしていません。
- 課題62の検査を弱めずに使い、自転車でだけ旧形式を明示的に扱いました。

## 検査結果と公開判断

課題54の自転車見本を最新生成器で実行し、意見468件・論点6件を同じ集合で集計できることを確認しました。公開候補としては、以下の読み飛ばし19件を検出して不合格です。

- 取締り強化賛成: 3件
- インフラ整備優先: 4件
- 免許制要求: 8件
- 車道走行への不安: 4件

公開ページ、正典データ、分類結果、既存の再読成果は変更していません。CEO承認が必要な公開操作も行っていません。

次にやることは、この19件を編集部が本文再読する必要があるかを確認し、必要なら課題54の見本側で読了記録を整えることです。
