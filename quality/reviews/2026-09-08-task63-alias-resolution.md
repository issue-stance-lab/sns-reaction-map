# 課題63：同じ本文・分類を持つ別投稿ID 272件の扱い

2026-09-08。同文別ID 272件は、元IDと証拠への対応先を全件確定した。**272件を確認済みへ読み替えず、IDごとの発言者・引用・文脈の確認を必要とする専用工程へ固定する。** 今回は本文分類を行わず、新規本文確認・読了継承・予約・既存作業台帳変更はすべて0件。公開意見の未確認残数は5,134件を維持する。

## 対応関係の実数

[本文なし再集計](2026-09-08-task63-alias-resolution.json)。棚卸しの除外理由と、作業台帳8,080件を4,231個の固定証拠から再構成して照合した。

| 別IDの区分 | 別ID件数 | 対応する元ID | 処理先 |
|---|---:|---:|---|
| 既存作業と同入力 | 262 | 確認済み60 | 元証拠への参照を保ち、別IDの帰属を確認 |
| 未確認集合内で同入力 | 10 | 新規候補4,769内の9 | 代表IDの完了に依存し、別IDごとの帰属を確認 |
| 合計 | 272 | 69 | 全件未確認のまま |

262件は全件、同じテーマ・本文ハッシュ・分類ハッシュ・基準版の元IDがちょうど1件だった。旧未完80件への依存は0件。10件も全件、4,769件側の代表IDへ一意に対応し、そのうち1つの代表には別IDが2件つながる。全272件で元IDと別IDの現行正典の存在、別々のIDであること、本文・分類ハッシュ、基準版を照合した。非公開テーマへの対応やテーマをまたぐ本文一致は使っていない。

| 元の最終判断 | つながる別ID数 | 元IDの実数 |
|---|---:|---:|
| 採用候補 | 62 | 15 |
| 保留 | 117 | 31 |
| 追加根拠待ち | 83 | 14 |
| 合計 | 262 | 60 |

作業台帳の初回提案区分では保持候補133・変更候補12・hold117だったが、これを最終採用状態に読み替えていない。過去4,000件の最終状態は既存採用候補の記録、今回4,000件は最終波別記録を参照した。**元が保留・根拠待ちの200件も元の本文確認は完了しているが、別ID272件の本文確認は未了である。**

| テーマ | 既存元IDへ | 未確認代表へ | 合計 |
|---|---:|---:|---:|
| AI著作権 | 2 | 1 | 3 |
| 憲法改正 | 36 | 0 | 36 |
| 消費税減税 | 40 | 6 | 46 |
| 副首都 | 42 | 0 | 42 |
| 皇室典範 | 142 | 3 | 145 |
| 合計 | 262 | 10 | 272 |

保存された `user_id` は同値266件・相違6件。空・null・unknown等の欠落や代替文字列は0件だった。同値はメタデータの一致を示すだけで、同一人物や同一発言の認定ではない。引用元の意見、記事見出し、動画紹介、投稿者自身の意見の区別は今回行っていない。全272件の `attribution_verified` はfalseを保持した。

## 後続工程で固定する条件

1. 既存入力の262件を通常の新規本文選択へ戻さず、別ID照合の工程を作る。元の60件をもう一度新規読了へ数えない。元IDの証拠を読む前に、別ID本文と発言者・引用・必要文脈から独立値を保存し、その後に比較する。証拠が不十分なら保留・追加根拠待ちへ残す。
2. 残集合内10件は9代表IDの処理を待つ依存関係を固定する。代表が完了しただけで別IDを完了にしない。1テーマ20件以下で、別IDごとの照合結果・理由・担当・監査と元IDの証拠参照を保存する。
3. 投稿単位の完了キーは `theme + post ID + body hash + classification hash + criteria hash` とする。入力単位のキーは `theme + body hash + classification hash + criteria hash` と分離する。同じ本文を持つ複数IDの確認で、入力の新規読了回数を重複加算しない。一方、各IDの完了証拠を持たないIDを未確認残数から差し引かない。
4. 状態と件数を「新規本文入力」「別IDの確認完了」「独立監査」「既存証拠参照」に分ける。元の採用候補を別IDへ自動転記しない。保留・追加根拠待ちはID別の正式登録後に未読へ戻さず、賛否集計への適用と分ける。
5. 入力・基準版が変われば同じ入力として再利用せず停止する。皇室145件も基準の独立検証を待ち、副首都42件の地域誘致反対を全体賛否へ混ぜない。

この文書は対象の処理先確定であり、後続の本文・帰属確認や分類体系変更の完了ではない。元データ、採用台帳、公開集計・ページは未変更。

## 証拠と検査

ID別対応先・元の最終状態・証拠参照はリポジトリ外の次のファイルに保存した。

`/Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/body-review-inventory/20260908-scope-resolution/alias-links-v3.private.json`

最初の保存版とv2のリンク・サマリも保持した。v2は保存user_idの比較名と欠落・代替文字列の区別を明示した版。v3は削除予定worktreeへの絶対参照を、保存区分（repository/private/canonical_repository）・相対パス・SHA256の安定参照に置き換えた版。840参照を照合し、対応ID・本文ハッシュ・分類・最終判断・件数は旧版と不変。canonical_repositoryは共有リポジトリの正典を指す。

v3のSHA256は `a9cc4ce7d9ac70b94ddc82d8b097369ec034db1858967fb06613576f538b5176`。

再現コードは `scripts/resolve_body_review_aliases.py`。13テストで同テーマ制約、本文・基準変化、複数元ID、同一ID・重複ID、最終判断欠落、監査待ち、元の保持候補から採用を推測しないこと、旧未完の非昇格、複数別IDからの読了非加算、空・代替メタデータの扱いを確認した。実データの8,080作業記録・272対応・全11正典ハッシュ照合は合格した。
