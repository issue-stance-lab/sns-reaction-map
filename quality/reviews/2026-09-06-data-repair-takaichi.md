# 63-M3 高市文春問題 — 候補作成済み／未適用

2026-09-06、main `3887c98` から専用作業ツリーで調査。`origin/main` と一致を確認。

取得履歴の欠損140件をすべて証拠付きで埋めた非公開候補を作成した。8/29保存回134件は、元の指示で累積正典を更新しないことが明示されていたため、全件を「意図的保留」として記録した。採用漏れ確定は0件。共有正典・main・公開HTML・設定・共通コードには未適用。

## 取得履歴140件

| 項目 | 原本 | 候補 |
|---|---:|---:|
| 投稿数 | 761 | 761 |
| 関連数 | 729 | 729 |
| 意見数 | 621 | 621 |
| fetched_at / query / source の欠損 | 各140 | 各0 |
| 本文・分類変更 / 投稿追加 | — | 0 / 0 |

原本 `takaichi_realtime_samples.json` の同ID140件は、Yahoo検索の強調タグ `\tSTART\t` と `\tEND\t` だけを取り除くと正典本文に完全一致した。`takaichi_realtime_ollama_batch_classified.json` では正典本文に完全一致し、rawの取得日時・検索語・取得元の組も全140件で一致した。URLも一致する。検索語・取得元をテーマ設定やファイル名から推測していない。

旧分類ファイルには同IDで異なる検索語等を持つ行もあるため、値を多数決で選ばず、当該rawに保存された一意の観測の組を採用した。旧分類にある同ID全行、再取得日がある21件を含め、参照パス・SHA-256・JSON位置・本文一致種別・全候補値を `fetch-dispositions.json` に保存した。最初に実際に取得した日・正典に採用した日の断定ではない。合成データやAI要約を本文照合の代替には使っていない。

`social-samples` の344 JSONを機械照合し、読めないファイルは0。関係する19原本は共有側とハッシュ一致。正典はGit復元、非公開更新回等は `private-data-20260906T095255889147.tar.gz` から復元した。共有側が新しい差分はなかった。

## 8/29保存回134件

raw147件、既存重複13件、新規分類134件、関連127件、意見110件。現行正典とのID重複は0件。rawから既存IDを引いた集合が分類134件と一致し、本文（Yahooタグ除去後）・取得3項目もrawと一致する。reportの `validated`、全検査true、分類エラー0件を再確認した。

元セッション `01a04dff-640f-75a3-8ef7-fe94cc0ca08b` の明示指示は「公開HTMLと累積正典は更新しない」。終了近くの記録にも761件維持・134件保存とある。セッションはコミット直前に中断され、`task/dashboard-22c50b2eac92` に `THEMES.yaml` と仮名化8/29履歴の未コミット変更が残る。変更や再開操作はしていない。元作業ツリーのraw・classified・reportも復元物とハッシュ一致した。

134件は個別の編集除外を意味せず、収集だけで保存する回としての保留。全IDへ理由・参照ログの指紋を付けた。今回の判断に全本文再読・再分類は不要だった。採用を改めて提案する際は134件のテーマ関連性、意見判定、現行論点・賛否との適合を本文で確認する。現在のラベル検査合格を意味の再確認とは扱わない。

賛否（批判・追及／擁護・懐疑／慎重・保留／中立・情報）は正典329/249/38/5で前後不変、保留回60/45/4/1。正典の論点件数は315/105/37/41/122/1、保留回56/18/8/9/19/0（中傷動画／文春／松井／比較／トークン／その他）。候補へは追加していないため主張照合・再読・潮目の対象集合も不変。保留回を将来採用する場合はこれらと期間を再評価し、今日の新着として扱わない。9/1保存の別ブランチ `50e3a66` は確認のみで対象外。

## 検査と残る判断

- ID一意・順序一致、取得3項目以外の全値一致、既存取得値維持、両作業の未処理0件を確認。
- 候補と保存回の既存分類検査、既存集合検査は合格。検査内の895件は仮に結合した場合の期待数であり、今回候補は761件。
- 候補を専用作業ツリーの正典位置へ一時配置し、高市のみ `verify_theme_page.py` 合格、`verify_number_provenance.py` は64件中64件説明可能。終了後は原本バイト列へ復元。
- `verify_sample_periods.verify()` を高市だけへ絞り、候補から再集計して実行すると **exit 1**。台帳unknownに対し保存日時文字列の範囲は2026-06-13〜2026-08-19となる。UTC表記と日本時間の扱い、取得履歴と調査開始の区別を確認して期間の扱いを決める必要がある。検査を緩めず、unknownは維持した。**このため公開前検査が全部合格した候補とは報告しない。**
- 新ページ再読検査は未実施。`configs/planet/takaichi.yaml` がなく、新ページ・再読成果を変更していない。
- `published: unlisted`、noindex、サイト内導線・sitemap除外は維持。収集予定日・公開更新日も進めていない。

## 保存先・指紋

非公開一式: `/Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/takaichi/20260906-m3-v2/`

原本・候補・全投稿の処置・観測履歴・19参照原本・セッション抜粋・検査・入力比較・再現コードを保存。30ファイルのコピー前後SHA-256一致、別の一時フォルダへの全ファイル復元一致、保存先の原本だけを使った候補と両処置一覧の再生成一致を確認。保全結果は `preservation.json`。別マシン復元ではなく同一マシン内の別フォルダ検査。

| 内容 | SHA-256 |
|---|---|
| 原本761件 | `cdad5a2196712c81650df45bad105f2bf1a8c983c2ddd101852e5f2dfa90fddb` |
| 修復候補761件 | `aee97f77330a187e3264a31a35ce9736c953cbfaae73979972bb0e96e39e0d74` |
| 旧raw | `d3321e174a0549c6b045a3e8cad13129e58b33617109327f3bd5dd882e79ad69` |
| 旧分類 | `104a022de11263e61b8488c62ac5ae6b7fc45286f4e9f49fc6b326b51e58392e` |

保存先から非公開候補を再生成するコマンド（既存保存回は上書きしない）:

```sh
python3 quality/reviews/2026-09-06-data-repair-takaichi.py \
  /Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/takaichi/20260906-m3-v2/sources \
  .staging/data-repair/takaichi/reproduced-63m3 \
  --session-evidence /Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/takaichi/20260906-m3-v2/session-context.json
```

成功の形: 欠損は各0、修復140、意図的保留134、未処理0と出て、candidate.jsonのSHA-256が上表と一致する。対象既存検査も再現する場合は、同じmain版の専用作業ツリーへ `sources/` 内のファイルを復元し、root引数をその作業ツリー、出力先を新しい場所にして `--verify` を付ける。共有作業ツリーでは実行しない。

## 統合担当への状態更新案

63-M3: 「取得履歴140件は候補作成済み／未適用。8/29の134件は収集のみ指定に由来する意図的保留と確認。未処理0。取得期間の扱いと正典適用は次工程」。課題28へ期間検査の不一致を引き継ぐ。課題40の7/26統合済み140件とは別の取得履歴欠損140件であり、混同しない。次は統合担当が期間の扱いを確定した上で修復候補の適用を判断する。
