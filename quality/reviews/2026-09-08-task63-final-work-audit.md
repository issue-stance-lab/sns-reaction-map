# 課題63：今回4,000件の作業登録を独立監査

**判定：PASS。** 保存元の200組と追加監査を再計算し、作業台帳8,080件への登録結果と一致した。新たな5,134件の本文確認・予約は0件。未確認はテーマ別投稿5,134件のまま保持する。

| 確認した項目 | 保存ファイルからの実数 |
|---|---:|
| 今回runの編集判断 | 4,000件 |
| 通常の独立監査 | 1,548件 |
| 通常の採否確認ファイル | 200組 |
| 追加監査 | 338件（以前完了57＋今回281） |
| 採用候補 | 2,159件 |
| 保留 | 1,251件 |
| 追加根拠待ち | 590件 |
| 追加監査待ち | 0件 |
| 旧作業台帳から全項目を保持 | 4,080件 |
| 新規に一意追加した作業記録 | 4,000件 |
| 登録後の作業台帳 | 8,080件 |

各波の元判断から `editorial_cycle.collect` で再計算し、指定された補足監査のみを元の判断との完全一致確認後に重ねた。4本の波別報告はすべて保存結果と一致。`load_registry` で出所の指紋を照合して台帳を再構築し、旧4,080行は一部項目だけでなく全項目がそのまま保持されていることを確認した。旧試行の未正式判断80件も別枠のまま残る。

新4,000件はテーマ・投稿ID及び本文・分類版に重複なく、全件が次回選択の除外対象になった。開始記録だけ、下書きだけ、完了ファイル欠落の組は0件。通常監査は編集担当と別の担当・作業場所であり、追加監査も元の編集担当とは異なることを照合した。担当拘束は運用上の記録であり、暗号学的な実行者認証や本文理解の証明ではない。今回の台帳監査を新規本文確認へ重複加算しない。

作業台帳の「維持候補2,318・修正候補536・保留1,146」は編集時の処理先で、上表の監査後採否とは別の項目。採否は波別の作業報告に保存され、採用台帳へ適用していない。追加根拠待ちや保留を次回選択の未読へ戻す処理も行っていない。

原本11テーマのハッシュは棚卸し時と一致し、今回runが固定した10テーマとも一致。採用台帳のハッシュは実行前と同じ。専用作業コピーと親作業コピーの `docs/` 237ファイルは集合・内容とも同一。元データ、採用台帳、公開ページの変更は0件。

今回4,000件の公開意見入力は1,923件。残る5,134件はテーマと投稿IDの組で、全テーマ共通の投稿ID一意数は5,133件。新規予約の対象候補は除外後4,769件で、差365件を確認済みへ振り替えていない。対象数が指定値と一致しないため、新しい確認工程は開始していない。

実数・ハッシュ・波別内訳は[監査JSON](2026-09-08-task63-final-work-audit.json)。非公開証拠は外付け `data-repairs/body-review-inventory/20260908-finish5134/final-work-audit.private.json`、読み取り専用の再現コードは同じフォルダの `audit-final-work.py`。コード監査の軽微指摘「結果ファイルの衝突検査より前に作業台帳へ書き込む」は `7e5c924` で解消し、回帰テストの合格を独立確認した。
