# 課題54・63：自転車の既存再読成果を新ページ設定へ接続

2026-09-06。基準main `a3a6557`。新規再読・分類・収集・公開は行っていない。

本文再読の根拠がある **206件を、6論点すべての新ページ設定へ接続した**。
これまで設定へ接続されていなかった「その他」の63件も含む。正典の本文・分類は変更していない。
旧ブランチの「236件読了」に混ざった自動分類93件を除外し、143件＋63件＝206件とした。

| 論点 | 現行意見 | 接続した編集再読 | 編集再読の記録なし |
|---|---:|---:|---:|
| その他 | 213 | 63 | 150 |
| 取締り強化賛成 | 90 | 3 | 87 |
| インフラ整備優先 | 58 | 54 | 4 |
| ルール曖昧・不信 | 58 | 58 | 0 |
| 免許制要求 | 29 | 12 | 17 |
| 車道走行への不安 | 20 | 16 | 4 |
| 合計 | 468 | 206 | 262 |

「編集再読の記録なし」には、棚卸しで別枠にした限定目的の本文確認29件も含む。
誰も本文を読んでいないとの断定ではない。自動分類93件中にも限定目的の本文確認6件はあるが、論点別の編集再読へは昇格させていない。

## 引継ぎの根拠と変更

- 旧反対再読の180件は `data/bike-blue-ticket_opposition_reread.json` をそのまま入力とする。
- 旧ブランチ `70f8792` の追加26件のみ、IDと既存の編集区分を `data/bike-blue-ticket_editorial-supplement.json` へ継承した。本文、URL、分類器の要約は転記していない。
- 新しい専用生成器 `scripts/build_bike_editorial_reread.py` は、上記2根拠から `data/bike-blue-ticket_issues-reread.json` を毎回組み直す。自動分類を含む数値出所配列は入力にしない。
- `configs/planet/bike-blue-ticket.yaml` は旧ブランチの投票順・論点IDを維持し、6論点を接続。固定件数の説明を置かず、件数は入力から算出する。
- 数値の出所ファイル `data/verification/bike-blue-ticket-reread.json` の既存273行は保持した。180行を `editorial_body_reread / body_reviewed: true`、自動分類93行を `automated_classification / body_reviewed: false` と明示。旧ページの件数は変わらない。
- 旧ブランチの背景台帳を継承し、編集部AIの既存成果であることと、今回一次資料を再確認していないことを明記した。人間が確認したという旧記述は引き継いでいない。

## 公開ゲートは不合格を維持

親作業の共通生成器変更 `4ec4e03` を取り込んで検査した。取得日不明116件の履歴候補は採用していない。
旧180件の記録にある `assigned_at: 2026-08-17` は増分再読の日付へ追随しておらず、180件すべての再読日を確定できない。
追加26件の実再読日2026-09-06と今回の接続日を別に保存し、旧180件を今回読んだと書き換えていない。

このため、設定の `review_date_uncertain: true` により未再読262件を**読み飛ばしか再読後増分か不明**として残した。
共通生成器は5論点の不明分を検出して停止した。「ルール曖昧・不信」は58件全件に再読根拠があるため不明分0件。
公開ページも公開用JSONも変更していない。今回の成果は接続準備であり、公開品質合格ではない。

## 検査と保存

- 棚卸しで確定した編集再読ID集合と生成206件が完全一致。自動分類93件との重複0件。
- 同じ入力で専用生成器を2回実行し、出力がバイト単位で一致。
- 正典のSHA-256は棚卸し時の `d31918a74541532ab99a5d5db589df4a48b1cc02b78d3f77746f7b172ab6463f` と一致。
- 自転車関連19テストが成功。自動分類の読了化、ID重複、正典外ID、非意見IDを検出する回帰検査を追加。
- 全11テーマの数字出所検査が成功（NG 0件）。
- 通常の新ページ生成は、時期不明262件を示して終了1。ゲートを緩和せず、試作フラグによる回避も行っていない。

非公開の元ブランチ成果、照合集計、検査ログは `/Volumes/HD-LE-B/issue-stance-private-backups/data-repairs/connect-reread/20260906-v1/bike-blue-ticket/` に保存。
次は、この接続成果を他テーマと統合して独立監査する。
