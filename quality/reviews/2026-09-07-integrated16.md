# 修正16件と確認記録の統合

2026-09-07。ロードマップの最初の統合作業を完了。これは既存記録の統合であり、新規本文確認は0件。

- 本文確認候補140件：維持69・修正16・保留55。
- 作業台帳220件：上記140件と正式判断未了の試行80件。限定監査10件は範囲付きの補足として保存し、正式判断へ格上げしない。
- 正典適用0、登録再読加算0。AI間の一致を正解保証にしない。

## 候補の検査

支持16件を一つの外付け非公開コピーに統合し、10テーマのページ・数字・トップ・公開JSON、投票・計測・OGP、2回生成の一致を検査した。元データは不変。新たな取得期間や公開状態の変更はない。

[判断と生成検査](2026-09-07-integrated16-results.json)、[復元内容を照合した保全記録](2026-09-07-integrated16-preservation.json)。本文と生成候補は外付けの `data-repairs/body-review-pilot/20260907-integrated16-v1` と同名アーカイブに保存。

## 次の担当の進め方

`scripts/prepare_next_editorial_batch.py` は `data/verification/editorial-work.json` を既定で読み、根拠26ファイルの指紋を確認して既存220件を除外する。実データの次20件に既存との重複がないことを確認済み。本文・分類・判断基準の版が変わった場合は旧判断をそのまま再利用しない。現在の選択処理は元データ全体の版が変わると停止するため、差分の点検と対象更新が先に必要。

担当中packetの予約はまだ台帳へ自動追記されない。担当を並列に割り当てる前に、親担当が順番に対象を準備し、先に予約したpacketを `--exclude-packet` へ渡して重複を防ぐ。正式判断未了80件と保留55件は別の未完了一覧として扱い、自動再送しない。追加の文脈・基準を得て再開するときに明示的に割り当てる。

まず累計1,000件へ20件×5回で進め、生成検査は100件ごとにまとめる。新規件数、維持・修正・保留、独立監査範囲、経過時間、計測できたトークンを毎回保存する。今回ローカルモデル呼出し0、Codexの親・子トークンは未計測。全1万件の時間・費用はまだ確約しない。

検査：全687テスト合格。台帳は親の作業コピーでも実根拠から再構築一致を確認。統合候補1,827ファイルはアーカイブ内の全内容を指紋照合済み。作業ブランチに保存し、共有mainへのマージと公開は未実施。
