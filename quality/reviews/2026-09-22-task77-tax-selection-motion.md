# 課題77 — 見本に合わせた選択時の表示と動き

実施日: 2026-09-22。オーナーの追加依頼による修正。未公開・pushなし・実投票なし。
工程6候補を更新する。公開判断 `approval-20260922-001` はpendingのまま。

## 比較したものと変更

参照は会話内の `consumption-tax-connected-layout.html`（「意見は、どこで分かれている？」）。
保存先は `/Users/studio/.codex/visualizations/2026/09/22/01a0c76d-ac20-7412-968d-98a16c75fe2c/`。
修正前は `.staging/task77-connected-stage6/` の固定候補。

| 観点 | 修正前 | 今回 |
|---|---|---|
| 操作と山の距離 | 冒頭バーと山側に立場ボタンが重複し、両者が離れる | 既存バー・5ボタン・山・7論点を一続きに集約 |
| 選択の見分け | 論点ごとに色が違い、非選択も濃い | 山は選んだ立場と同色。選択0.9、非選択0.23の濃淡。押下状態・数値ラベルも表示 |
| ボタン | 選択は細い枠線 | 濃紺の背景と白文字で強調。件数を別行に表示 |
| 山の変形 | 約500ms、前後をゆっくり変える | 見本と同じ480ms、すぐ変わり始めて終わりをゆっくりにする |
| 軸・大きさ | 60%上限、PCで340px高 | 0〜100%の固定軸、PC210px・狭い画面180px。選択した山に件数と割合 |
| 選んだ立場の説明 | バー側の説明から山へ移動するリンク | 山の直前に最多論点と件数。選ぶと同じ場所で論点が切り替わる |

新しい質問・固定した架空の集計は追加していない。既存の予想2問を再利用する。
数値はPLANET_DATAから取得。山と論点の順序は固定し、0件の山は描かず、0%の強い表現は基準線上の点で示す。
小さい山の最小幅補正を注記し、正確な件数はボタンで読める。動きを減らす設定では即座に切り替わる。
全体表示の山はサイトの青、立場選択時は既存バーの色。見本の紫へサイト全体の色を変える変更ではない。

理由分類・投稿例・資料は論点全体の内容であり、立場別の再分類とは扱わない。
理由25投稿、資料3タブ、資料側4項目、資料クイズ6問、年表、進捗、引用・授業・印刷・投票28通りを維持。
原稿・元データ・確認日は変更せず、JavaScriptなしの静的本文も残す。

## 検証

- 専用の生成・部分更新32テスト: 合格。
- Chromium / WebKit、1280 / 375 / 320px: 全7論点×5立場の接続と数値、0件、同率、キーボード、図解、初期リンク、JavaScriptなしを確認。
- 通常の動きで中間の形を観測し、連続切替が最後の選択へ収束することを確認。
- 新規の操作検査: 5ボタンがバー内に1組だけ存在、立場切替でスクロール・山の位置が変わらない、色と濃淡、右端のラベル、山・最多論点ボタンの焦点保持。両エンジン3幅で合格。ボタン背景の切替も共通の遅延を外し、クリック直後に選択中だけが紺・白文字になることを検査した。
- 理由25項目の開閉・投稿読込・遅延読込・失敗時の元リンク・再試行、資料3タブ、年表3日付、6問のクイズ、引用・履歴・進捗・印刷が合格。
- 投票28通り・送信失敗後の再試行・既存保存番号の互換性をテスト応答で確認。実投票0。
- 数値出典: 消費税220/220、説明不能0。テーマ構造・文体検査が合格。
- 公開用検査: 最終候補で914件合格（既存4件スキップ）。JSON・SEO・期間・構造・出所・保全台帳も合格。
- 独立監査: `tax_independent_audit` による再監査はready_for_ceo。両エンジン3幅、全5立場の件数・100%軸に対する実描画高さ・色、7論点のEnter/山のSpace、0件/全件0、縮小動作、連打中の拡縮で合格。PLANET_DATA・全読書テンプレート・投票fingerprintも前候補と一致。

公開用検査で、旧テストが「承認待ちは全案件で0件」を仮定していたため、本課題の承認待ちを誤検知した。
完了済みnote案件が承認待ちへ戻らないという本来の検査に限定して修正した。承認台帳の状態は変更していない。
初回検査中のコード整形で候補と生成元に一時的な差が出たため、両者を再固定して再実行した。
独立監査で、WebKitの変形途中に375→1280pxへ拡幅するとResizeObserverの循環通知エラーが出る点を検出。
サイズ変更後の描画を次のフレームへ送り、両エンジンで連続20回の切替中に拡幅する再発検査を追加して合格。
監査中の「非選択ボタンが紺のまま」という指摘は120msの色変化の途中で、永続的な不具合ではなかった。見本に合わせてその遅延を外し、クリック直後の色も確認済み。
実端末・公開先の確認、全投稿の現在のX配信可否の再確認は未実施。外部埋込の機能検査はテスト応答で行った。

## 最新候補と引き継ぎ

保存先: `.staging/task77-tax-selection-motion/`。候補HTML、表示4ファイル、manifest、修正前後の画面、検査ログを固定する。
確認URL: `http://127.0.0.1:8795/consumption-tax-cut-reaction-map.html#modes`。
候補HTMLのSHA-256: `27b2632daaadbb4c5c173f14626a7c27fe82c224b03ed4f2dfcf23ffd461d530`。表示ファイルの指紋もmanifestに記録。切替前の公開HTMLのSHA-256は `f3cf1d07566b83781bc311842da831b95d734c7ade19f989152ef1c32fa02ca7`。
参照版はv8。公開HTMLは未切替のままで、共有ツリーの確認用サーバーだけが最新候補を表示する。

次はオーナーが立場・論点を切り替えて見やすさを確認する。
公開の承認を受けた後は[工程6の公開手順](2026-09-22-task77-tax-release-ready.md)を継承し、今回の最新候補を使う。
正典・本文がその間に更新されていれば、古い候補を上書きせず最新の生成元から作り直す。
確認画面だけを戻す場合は `.staging/task77-connected-stage6/` のサーバーに戻せる。

今回の検査基準は `08501162`。作業中にmainへ部活動の更新が追加されたが、消費税の公開HTMLは変更されていないことを確認。統合後・公開前の全体検査は別途行う。
