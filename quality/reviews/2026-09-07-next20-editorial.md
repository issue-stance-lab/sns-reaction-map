# 新規20件の限定編集確認（第1回）

2026-09-07。既処理100件を除いた20件を新たに確認。結果は維持候補8件・修正候補3件・保留9件。既存20件の判断整理とは別の新規確認である。正典適用・段階Cの登録読了数への加算は行っていない。

編集担当1名が20件を確認し、独立担当1名が全修正候補4件＋維持の抜き取り2件＋保留の抜き取り1件を確認した。修正4件のうち3件を支持、1件は分類値が同じでも判断保留のため採用候補から外した。もともとの保留1件では独立担当の判定が違ったが、保留を勝手に解除していない。全員一致や全件二重確認と表現しない。

| テーマ | 支持された変更 | 意見投稿数（候補） |
|---|---|---|
| 消費税減税 | 1件を意見ありへ | 3,358 → 3,359 |
| 生成AI著作権 | 1件を意見あり・規制制限強化支持へ | 2,593 → 2,594 |
| 憲法改正 | 1件を意見ありへ | 1,372 → 1,373 |

判定の根拠本文は非公開に保存。[20件台帳](2026-09-07-next20-editorial-results.json)、[負担測定](2026-09-07-next20-metrics.json)、[隔離生成検査](2026-09-07-next20-candidate-generation.json)。

3件だけを別コピーへ反映し、2回生成同一、テーマ別件数・ページ数字・トップ・公開JSON整合、投票互換・計測・OGP維持を検査した。最初の生成は公開集計を更新する順序が遅く、消費税ビルダー内の整合検査で停止した。公開集計を先に生成する順序へ訂正し、別コピーで再試行して全検査に合格。失敗ログも保持した。既存ビルダーの検査を緩めていない。

編集担当の記録時間は約65秒、独立担当は約39秒。親の準備・生成検査を含む予約開始から生成確認までは約392秒（保全処理・最終記録は別）。ローカルAI呼出し0。Codex親・子のトークンは未計測であり、消費0や全体節約率と読み替えない。今回だけで1万件の時間・精度を推定しない。

判断前後に共有mainの20件の本文・4分類・基準を照合し一致。20件の判断実施実績は残すが、登録読了数の変更や候補適用は別工程。保留・候補・適用済みを混同しない。

保全先は非公開外付け `data-repairs/body-review-pilot/20260907-next20-review-v1`。次の未確認20件は、今回の20件も除外して `20260907-batch02-prepared/packet.private.json` へ準備済み（未判定）。

前回支持2件と今回支持3件で、未適用の修正候補は合計5件。次はこの5件を一つの非公開候補にまとめ、公開判断用の検査結果をそろえる。
