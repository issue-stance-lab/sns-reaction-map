# 課題63：差365件の処理先を確定

2026-09-08。オーナーの「確定作業を開始」に基づき、前回の差365件を投稿IDと証拠で照合し、全5,134件の処理先を確定した。今回の確定は処理対象と依存関係であり、本文の読了・分類・採用の確定ではない。未確認は5,134件のまま、新規本文確認・ID別確認完了・実行予約は0件。

## 確定した区分

| 区分 | 件数 | 次の処理 |
|---|---:|---|
| 通常の未確認入力 | 4,769 | 新しい担当割当で本文を確認する |
| 旧試行の正式判断未了 | 63 | 旧補助回答・限定監査を残し、正式な本文確認を行う |
| 無効実行の未着手予約 | 30 | 無効予約を履歴として残し、別の新規実行で本文を確認する |
| 既存確認済み入力と同文の別ID | 262 | 既存60投稿を参照し、別IDの本文版・帰属・文脈を個別照合する |
| 未確認集合内で同文の別ID | 10 | 4,769側の9代表投稿の確認を待ち、別IDを個別照合する |
| 合計 | 5,134 | 各IDの完了証拠が揃うまで未確認に保持 |

本文を新たに判断する入力は4,769＋63＋30＝4,862、別IDの照合は272。既存の60入力を含めた対象の本文・分類・基準版は4,922組となる。全5,134はテーマと投稿IDの組で、全テーマ共通の投稿IDは5,133件。これらは異なる母数であり、同一視しない。

差365件を通常の選択処理へ無条件に戻して5,134件へ帳尻合わせすることはしない。63件と30件は再開の専用区分、272件は別ID照合の専用区分へ固定する。旧作業台帳8,080件の状態・証拠、既存の無効記録は変更しない。

## 93件の再開条件

[独立調査の結果](2026-09-08-legacy93-resolution.md)を取り込み、親担当の処理台帳と全93件の投稿・本文・分類・基準版・再開先が一致した。

旧63件は全て `attempted`（正式判断未了）。自動補助回答は存在するが、形式が有効な13件も本文確認済みへ読み替えない。形式不合格50件も保持する。うち4投稿の限定監査5所見も旧証拠として引き継ぐ。

無効予約30件は旧4,000件v1/v2に記録されており、該当する各19組はpacketだけで、編集・監査の開始、下書き、完了の記録が無かった。無効な実行から読了を継承しない。履歴を削除せず、新しい実行IDへ旧予約との関係を明記する。

今回は再開先を確定しただけで、稼働中の新しい担当へ予約していない。実行直前に現在の原本・分類・基準版と担当中の集合をもう一度検査し、保存済み結果へ上書きしない。

## 同文別ID272件の確認条件

[対応関係と元の最終判断](2026-09-08-task63-alias-resolution.md)。既存60投稿の判断を別IDへ自動コピーしない。262件が参照する元判断は、別ID件数で採用候補62・保留117・追加根拠待ち83。元の保留や根拠待ち200件も本文確認は完了しているが、別ID自身の確認が済んだことにはならない。

保存user_idの同値266・相違6も、投稿者本人の意見と認定する根拠にしない。引用・記事見出し・動画紹介・投稿者自身の意見を分け、元の判断を見る前に別IDの独立値を保存してから比較する。別IDの全件に独立確認を必要とする。

同じ本文の入力キーと、投稿ごとの完了キーを分ける。1つの代表の本文確認を何度も新規確認へ加算せず、別IDの完了証拠がないまま未確認数を減らすこともしない。保留・根拠待ちは、各IDの正式な確認記録を登録した後でのみ未確認から外し、採用・公開の判断と分離する。

## 実行に残る条件

1,000件単位、本文を読む組は単テーマ20件以下、最後の134件もテーマを混ぜず20件以下を維持する。編集2担当と独立監査担当は別worktreeを使用し、変更案全件・維持案の標本・必要な追加監査を行う。進捗報告だけで終了せず、完了ファイルの実数で残数を確認する。

皇室782件（通常630・旧未完7・別ID145）は、[分類案v2](../designs/body-review/KOSHITSU_SEPARATE_AXES_V2_DRAFT.md)の独立検証が終わるまで着手できない。父系血統・男子限定・女性天皇・女系の区別を保つ。副首都の `scoped_only` も全体賛否へ混ぜない。

## 台帳・検査・保存

新設の `data/verification/editorial-review-scope.json` は、全5,134件の非公開処理台帳の指紋と集計を持つ。非公開台帳には各IDの区分、本文・分類・基準版、依存先ID、旧作業キー、未確認状態、着手条件を保存した。読了数・採用許可は全件0。既存の選択除外を緩める変更はしていない。

再現・検査は `scripts/resolve_remaining_review_scope.py`。旧未完の誤昇格、版違い、元ID欠落、依存先の曖昧さ、集合漏れ、重複、読了自動加算、処理先台帳と実行予約の混同を拒否する。独立担当の272件対応と全件一致し、93件の再開調査ともIDと状態を照合した。

[独立実装監査](2026-09-08-task63-scope-independent-audit.json)と[親担当の全件照合](2026-09-08-task63-scope-final-check.json)を保存した。関連38テストに合格し、参照証拠1,399箇所（186ファイル）のハッシュを照合。原本・採用・公開の保護対象261ファイルと既存作業台帳8,080件は不変だった。独立監査の提案2点（予約済み状態の拒否、非公開領域外への出力拒否）も反映・検査した。

今回の新規本文確認・独立本文監査・追加本文監査・採用候補追加・保留追加・根拠待ち追加・無効実行は全て0。実施したのは作業履歴・依存関係・対象台帳の監査である。元データ・採用台帳・既存作業台帳・公開集計・ページは不変。push・main統合は行わない。

[非公開証拠の保全・復元検査](2026-09-08-task63-scope-preservation.json)に結果を記録する。担当ごとの元証拠と参照ファイル、変更したリポジトリ文書・コードを別領域へ保存し、復元後の内容ハッシュを照合する。

次はこの区分を使って本文確認へ進む。皇室は基準v2の独立検証を先に行う。次回は `task/task63-scope-resolution` の処理対象台帳と8,080件の作業台帳を引き継ぐ。
