# 課題54: 3D「議論の惑星」を中心とするWebsiteリニューアル（最重要）


**状態**: 方針承認・設計確定。**2026-09-01 に照合レイヤー「海・地下水脈」を追加し、課題15-Bを本課題へ統合した**（オーナー承認済み）。段階0〜6完了（未マージ）。**2026-09-03の段階6レビュー指摘1〜6に対応済み。段階7着手可**
**次にすること**: `task/planet-stage6-fix`（段階6＋レビュー指摘1〜6の修正を含む）をオーナー確認のうえ`release`スキルでmainへ反映し、段階7（部活動ページの試作・非公開）へ進む
**優先度**: **最重要**。通常の新規テーマ追加、検索記事追加、旧2Dマップの横展開より優先する
**対象**: 公開対象10テーマすべて。最初の完成基準は `bukatsu-chiiki`（部活地域移行）。対象IDは設計書に固定する
**正典**: `quality/designs/reaction-planet-renewal.md`
**独立レビュー**: `quality/reviews/2026-08-30-reaction-planet-plan.md`
**AdSense対策**: `quality/designs/planet-adsense-gates.md`（2026-08-30。生成器に独自性の検査を実装。展開順の変更提案あり）
**部活の実装構想**: `quality/designs/planet-bukatsu-chiiki.md`（2026-08-30 作成。面積・色・標高の具体値を現行正典993意見で確定）

**目的**: テーマごとに雑多な情報を縦に並べるページから、読者が議論の全体像を回し、論点へ着陸し、
理由と一次資料を読めるWebsiteへ変える。noteは論考、Xは入口、Websiteは探索できる中心商品に役割を分ける。

**照合レイヤー（2026-09-01 追加）**: 惑星の陸地は「SNSで何が語られたか」で、これは収集と分類の仕組みが
あれば再現できる。独自性はそこには無い。海面より下に、**人が一次資料を読まないと出てこない情報**だけを置く。

- **沈んだ大陸** — 一次資料では争点なのに、収集した投稿にほとんど無い論点
- **地下水脈** — 対立する立場が共有している具体的な懸念（両側の代表投稿を並べて示す）
- **陸地の判定** — 一次資料と照らして `fact`（実像）／`gap`（ずれた海岸線）／`miss`（蜃気楼）

「無いもの」は集計できない。人が資料を読んで初めて分かる。この非対称が「AIが集めて分類しただけ」を
構造的に成立させなくする。仕様は設計書 3.3、書き方の基準は 3.3.2（重要度の名指しはしない）。

**変えない意味**:
- 球体全体 = 1テーマ
- 大陸・島 = 論点
- 面積 = その論点に分類された意見数
- 色 = その論点内のスタンス分布（テーマ固有ラベルを使い、単純な赤青へ固定しない）
- 標高 = AI分類から再現可能な「強い表現の割合」。正しさ・重要度・世論の熱量とは呼ばない
- 大陸の位置 = 初版では見やすさのための配置。論点同士の近さを意味しない

**読者体験**: 最初に惑星を表示し、回転（横・縦）、拡大縮小、論点への着陸、全体表示への復帰を可能にする。
着陸後は同じ画面内の読めるHTMLに、比率、代表的な理由、一次資料、編集部の整理を表示する。
3Dが動かない端末、JavaScript無効時、キーボード操作でも同じ主要内容へ到達できることを必須とする。

**前提（完了済み）**: 課題55で正式URLと公開元を一本化。課題57で意見数・論点数・更新日・用語の
公開データ契約（`data/public/`）を一本化。課題54はこの公開JSONとcatalogだけを構造化データ入力とし、
HTMLや設計書の固定数字を読まない。

**工程表**（各段階の「完了の形」を満たすまで次へ進まない。目安は所要のAIセッション数）:

| 段階 | 何をするか | 完了の形（検査で確かめる） | 目安 | オーナー |
|---|---|---|---|---|
| 0 | 設計書へ照合レイヤーを追記し、15-Bを本課題へ統合する | 設計書 3.3／4／11／12／13／14 が更新され、mainに入っている | 済 | 方向承認（済） |
| 1 | 判定語彙を `fact`/`gap`/`miss` の3語へ統一する | 6テーマ41主張が同じ語彙で読み出せる。憲法の日本語判定3種を写像。**公開ページのHTMLはバイト単位で変わらない** | 済 | 不要 |
| 2 | 照合データを公開契約へ載せる | `schemas/public-theme.schema.json` に照合項目を追加し、`verify_public_registry.py` が通る。未実施4テーマを「未実施」と機械判定できる | 済 | 不要 |
| 3 | 部活動の一次資料照合を行う（**15-Bの本体**） | 主張5〜8件について一次資料に当たり、該当投稿を1件ずつ読んで確定。`data/bukatsu-chiiki_claim_posts.json` を作る。空振りは空振りのまま残す | **済（2026-09-02、レビュー指摘の母数ズレ修正込みでmain公開済み）** | 不要 |
| 4 | 部活動の「沈んだ大陸」を抽出する | 設計書3.3.2の基準で最大4件。各件に一次資料URL・該当箇所・生活への影響・SNS件数と母数・確認日 | **条件付きpass（2026-09-02レビュー）。4件はオーナー確認済みだが件数の数え方3点を修正中** | 済 |
| 5 | 部活動の「地下水脈」を抽出する | 2〜4本。各本に両側の代表投稿ID（各2件以上、現行正典に存在）・共有する懸念・それでも分かれる理由 | **差し戻し（2026-09-02レビュー）。AI要約の組み替えで作られており本文の読み直しが要る** | 不要 |
| 6 | `build_planet_data.py` に海・地下水脈を接続する | 同じ入力で2回生成して差分0。公開JSONだけを入力にしている。台帳に無い論点の海面下が空で出る | 2 | 不要 |
| 7 | 部活動ページを試作する（非公開） | 惑星→着陸→読む の順。重複セクション（注目ポイント／論点カード／分布／潮目／詳細表）を統合。3D無し・JS無し・キーボードのみで同じ内容へ到達 | 3〜4 | 不要 |
| 8 | 「同じ数字は1回だけ」を検査にする | `verify_theme_page.py` が論点数・意見数・収集数の二重表示で止まる。自転車の「5つの論点／6つの論点」が検出される | 1 | 不要 |
| 9 | 品質監査を受け、共通仕様として承認を得る | 独立監査の判定 `pass`。LCP2.5秒・INP200ms・CLS0.1・375px横スクロール無し・JSエラー0を実測記録 | 1〜2 | **判断点。ここだけ必須** |
| 10 | 残り9テーマへ展開する | うち生成AI・辺野古・あだ名禁止は段階3から実施（一次資料照合が未実施）。10テーマすべてが同じ生成器で再生成でき、確認台帳に記録される | 8〜12 | 不要 |
| 11 | 総合監査 → 公開（15-C） | 10テーマ・トップ・sitemapの総合監査後、CEO承認を得て公開元へ反映。Search Consoleで新ドメインの最新版取得を確認 | 2〜3 | **公開承認（必須）** |
| 12 | AdSense再申請（15-D） | 10テーマの品質証拠と再クロール確認がそろってから、CEO事前承認を得て申請 | 1 | **再申請承認（必須）** |

**オーナーの出番は3か所だけ**（段階4の文言確認、段階9の共通仕様承認、段階11・12の公開/申請承認）。
それ以外はAIが進めて記録を残す。

**この工程表の要点**: 基準実装の部活地域移行は、一次資料との突き合わせが**未実施の4テーマの1つ**。
段階3を飛ばすと海面より下が空のまま基準実装が完成し、共通仕様の確認ができない。
段階1〜2（語彙と契約）を先に済ませるのは、6テーマ分の既存の照合結果を作り直さずに使うため。

**完了条件**:
- 10テーマすべてが1つの共通生成経路から再生成でき、同じ入力では地形が勝手に変わらない
- 10テーマすべてで陸地の判定（fact/gap/miss）が公開JSONから再現でき、テーマ別の語彙が残っていない
- 沈んだ大陸・地下水脈が、確認日と確認者種別を持つ確認台帳の記録だけから生成されている
- 沈んだ大陸の本文に重要度・優先順位の主張（「本当の問題は」「見落とされている」）が含まれない
- 同じ数字が1ページに2回以上出ていないことを検査で保証している
- 公開対象10テーマを設定から機械判定でき、公開HTMLを次回生成の入力にしない
- 10テーマすべてで、地形の面積・色・標高と読者向けHTMLの数字が正典と一致する
- スマホで横スクロールせず、縦回転を含む操作、戻る操作、キーボード操作、動きの抑制設定が機能する
- WebGLまたはJavaScriptが使えなくても、論点・比率・理由・一次資料・編集整理を読める
- 既存の投票、GA4、AdSenseコード、Supabase、OGPを壊さず、公開ページに内部のAI判定理由を出さない
- 10テーマの確認台帳、独立監査、公開前レビュー、再クロール確認がそろうまでAdSenseを再申請しない

**対象外**: 初版の銀河トップ、リアルタイム更新、投稿1件ごとの3D点群、VR/WebGPU、マップ操作内や直近の広告配置。
編集部が「本当の問題はこれだ」と重要度を名指しすること、一次資料に無い争点を推測で沈んだ大陸に置くこと。

**進捗（2026-09-01）**: 段階0完了。オーナーが照合レイヤーの方向を承認し、沈んだ大陸の書き方は
「一次資料にあり、かつ生活への影響が具体的に特定できるものに絞る」を既定とした（設計書3.3.2）。
段階4の試作を見て最終確認する。設計書へ 3.3／3.3.1／3.3.2／3.3.3 を追加し、4章の画面構造に照合と
「同じ数字は1回だけ」を、11章に照合レイヤーの検査6件を、14章にデータ契約項目を足した。
課題15-Bは本課題の段階3〜5へ統合し、別課題として並走させない。専用worktree `task/planet-ocean`。

**段階1（2026-09-01）**: 憲法改正の内部判定を、従来の日本語3語から `fact` / `gap` / `miss` へ写像した。
既存の日本語表示（「原典にある／原典とずれる／原典にたどり着けず」）は生成時に戻すため、公開HTMLは変更していない。
`python3 scripts/verify_claim_verdicts.py` と `tests/test_claim_verdicts.py` で、照合済み6テーマの判定語が3語だけであることを固定した。
工程表の「45主張」は実データと一致しなかったため **41主張**（自転車7・憲法10・消費税6・高齢者7・副首都5・皇室6）へ訂正した。
部活動は照合が未実施の4テーマの1つで、段階3の対象である（2026-09-01のレビューで「部活7」の誤記を訂正）。
検査記録: `quality/reviews/2026-09-01-task54-stage1-claim-verdicts.md`。次は段階2（照合データを公開契約へ載せる）。

**段階2（2026-09-01）**: 公開テーマJSONへ `claim_verification` を追加した。照合済み6テーマは主張、
`fact` / `gap` / `miss`、照合結果、一次資料URL、確認済み投稿数、確認日・確認者種別を生成する。
投稿ID・本文・AIの内部理由は公開JSONに入れない。未実施4テーマ（生成AI著作権・部活動地域移行・
辺野古学生事故・あだ名禁止）は、空欄ではなく `not_started` として機械判定する。非公開正典からの
10テーマ再生成・完全照合と、公開JSONだけのSchema／不変条件検査を通過。

**段階1・2のレビューと修正（2026-09-01）**: 段階1・2の成果物を実測で確認し（判定語41主張・
公開HTMLの再生成差分0・件数248件の一致）、指摘6件を修正した。段階3には未着手。

1. **主張と論点の対応を追加**（`issue_ids`）。3.3の地形は論点単位で塗り分けるため、対応が無いと
   段階6で色を決められず、段階3で作る部活動の主張も結び直しになるところだった。41主張すべてを
   各テーマの論点へ結びつけ、「その他」への割り当てを検査で禁止した
2. **設計書14章と公開Schemaの矛盾を解消**。14章の「該当投稿IDを持つ」を「件数だけを持ち、
   投稿IDの正典は `data/{theme}_claim_posts.json`」へ改めた（実装side が正しかった）
3. `fact` / `gap` の主張に一次資料0件を禁止（0件でよいのは `miss` だけ）
4. 確認者種別に `ai_assisted` を追加。AIの下読みだけの照合を「人が確認」と表示しないため
5. 主張ごとの件数を 確定データ / `data/verification/` / 公開JSON の3か所で突き合わせる検査を追加
6. 上の検査で、憲法の確定データだけ旧判定語（「原典にある」等）が残っていたのを発見し統一。
   旧語彙の再混入も検査で止める

記録: 修正後に `verify_claim_verdicts.py` / `verify_public_registry.py`（2モード）/
`verify_theme_page.py` / 全テストが通り、2回生成の差分は0。次は段階3（部活動の一次資料照合）。

**段階3の進め方（2026-09-01 整理。着手前）**: 段階3は**公開ページを触らず、データだけを作る**作業。
オーナーの操作は不要で、判断は着手タイミングの1点だけ。所要2〜3セッション。

前提として揃っているもの（ゼロからの調査ではない）:

- 一次資料メモは 2026-08-31 更新済み（`quality/research/bukatsu-chiiki-primary-sources.md`）。
  部活動指導員制度（学校教育法施行規則第七十八条の二を条文全文で照合済み）、国庫補助率、
  東京都の部活動指導員 時間額2,360円、阿賀野市の大会参加費補助の実額まで入っている。
  ただしメモ内に「PDF本文未確認」「推定」と自認した箇所が資料A・Dに残っており、段階3で潰す
- 現行正典993意見に材料がある（2026-09-01 に集計）。指導員・資格に触れる投稿91件、補助・国費63件、
  教員の残業・給特法47件、年度・期限19件、統計・割合11件、中体連・大会8件、金額7件。
  主張5〜8件は成立する見込み（3件も集まらなければ作らずに報告する、が `FACT_CHECK_GUIDE.md` の決まり）

作業ステップ:

0. 専用worktreeを main から作る（段階1・2の `task/planet-ocean` は main へ取り込み済み）。
   正典と `data/verification/` を rsync で揃え、着手前のテスト失敗数を記録する
1. 993意見から「事実として確かめられる主張」を5〜8件洗い出す。各主張に固定IDと論点IDを付ける。
   「その他」（`bukatsu-chiiki-sonota`）へは結び付けない（段階2の指摘1で検査が禁止済み）
2. 一次資料に当てて `fact` / `gap` / `miss` を付ける。引用は機械照合する（e-Gov法令API・
   国会会議録API・`curl -sI` でURL200）。空振り（`miss`）が1件も出ないときは調べ方が浅い疑い
3. 該当投稿を1件ずつ読んで確定し、`data/bukatsu-chiiki_claim_posts.json` を作る。
   キーワード抽出の件数をそのまま使わない（自転車で3〜4割の水増しが実際に出た）
4. 判定リテラルの置き場所を作る（**唯一の設計判断**）。`scripts/build_bukatsu_process_sections.py` に
   `FACT_CHECKS` / `CHECKED_AT` / `write_provenance_records()` だけを置き、HTMLへの差し込みは書かない。
   他6テーマは同名の生成器がページに事実確認セクションを描くが、部活動は段階7で惑星ページとして
   作り直すため、いまページを触ると段階1の「公開HTMLはバイト単位で変わらない」を破る
5. 件数の3か所をつなぐ。`data/verification/bukatsu-chiiki-claims.json` を生成し、
   `public_registry_common.py` の `CLAIM_AUDIT_SOURCES` と `verify_claim_verdicts.py` の `SOURCES` に
   `bukatsu-chiiki` を登録して、`python3 scripts/build_public_registry.py --all` で公開JSONを
   再生成する（`not_started` → `complete`）
6. 検査の期待値を直す。`tests/test_claim_verification_contract.py` が「未実施は4テーマ」と部活動を
   名指しで固定しているので3テーマへ。`verify_claim_verdicts.py` の「6テーマ」表示も7へ
7. 検査を通して記録を残す。`verify_claim_verdicts` / `verify_public_registry`（2モード）/
   `verify_theme_page bukatsu-chiiki` / 全テスト / 2回生成の差分0 / `git status` で `docs/` に差分が
   無いこと。記録は `quality/reviews/` と `THEMES.yaml` の notes と本欄

**段階3でHermes（分類モデル）を使える範囲（2026-09-01 確認）**: 使えるのは**候補の下読みまで**で、
確定には使えない。

- **使える**: ステップ3の候補ふるい（主張ごとに「この投稿はこの主張をしているか」を yes/no で1次選別し、
  残った候補だけを編集部が1件ずつ読む）。ステップ1の前さばき（キーワードでは拾えない言い換えの拾い上げ）も可
- **使えない**: 該当投稿の確定。公開JSONを `complete` にするには確認者種別が `editorial_review` である
  必要があり、`public_registry_common.py` の検査が `ai_assisted` を弾く。Hermesの出力をそのまま件数に
  すると段階3は完了しない。一次資料の照合（`fact` / `gap` / `miss`）もLLMの要約を信じない
  （`primary-research` スキル）。段階4の沈んだ大陸・段階5の地下水脈は、定義上AIに代替させない
- **注意**: `~/.hermes/config.yaml` の `model.default`（`kimi-k2.6`）はグローバルに効くので触らない。
  使う前に `pgrep -fl "classify_.*hermes|refresh_topic"` で他セッションの分類を確認する
  （2026-09-01 23時台は高市テーマの収集・分類が実行中だった）。下読みの出力は正典
  `social-samples/bukatsu-chiiki_hermes_classified.json` へ書き戻さない（件数と潮目が動く）

**着手タイミング（2026-09-02 オーナー決定: A）**: ステップ1〜2を先に進め、ステップ3の投稿確定は 9/3 の定例更新後に回す。

**段階3（2026-09-02 完了）**: **ステップ0〜7すべて完了。** 専用worktree `task/planet-stage3`（mainから分岐、未マージ）。
候補176件を1件ずつ読んで30件を確定し、主張7件（fact 2 / gap 4 / miss 1）を `data/bukatsu-chiiki_claim_posts.json` に固定。
公開JSONを `not_started` → `complete`（確認者種別 `editorial_review`）にした。**公開HTMLは1バイトも変えていない**（段階7で作り直すため）。
検査は `verify_claim_verdicts`（7テーマ48主張）/ `verify_public_registry`（2モード）/ `verify_theme_page bukatsu-chiiki` / 全368テストがOK、2回生成の差分0、`docs/` 差分なし。
記録は `quality/research/bukatsu-chiiki-stage3-claims-draft.md`。
**オーナー決定（2026-09-02）**: miss（BC-7 運動部費用 年8万4千円・保護者65%）は**そのまま載せる**。
**残っていること**: ①本番反映（`release` スキル）②9/3の定例収集で増える分の差分読み（`FACT_CHECK_GUIDE.md`「データを追加したときの扱い」）③次は段階4（沈んだ大陸）。

**段階3レビューと本番反映（2026-09-02）**: 独立レビュー（`quality/reviews/2026-09-02-task54-stage3-review.md`）で
条件付きpass。**指摘の核心**: 段階3は993意見（〜08-26）で確認したが、その後の部活動データ更新
（`caa9f31`/マージ`46da705`）で公開母数が1139意見（〜09-02）へ増えており、そのままマージすると
「新しい件数のページに古い時点の確認内容」が並ぶ。既存の検査（`verify_claim_verdicts.py`等）は
3か所とも同じ確定データから作るため、この母数ズレを検出できないことも実測で確認済み
（既存6公開テーマにも同種のズレが最大12日分見つかっている）。

**対応**: 増えた146件を7主張について同じ基準（願望・提案・仮定を除く）で読み直し、`coach-pay`
（指導者の報酬は最低賃金並みで人が集まらない）に2件追加（確定30件→32件）。他6主張は候補は
あったが断定表現でなく見送り。`task/planet-stage3` へ main を取り込み、公開JSON・検証データを
再生成してゲート一式（`verify_claim_verdicts` 7テーマ48主張／`verify_public_registry` 2モード／
`verify_theme_page bukatsu-chiiki`／全368テスト／`verify_number_provenance`／`verify_top_page`／
2回生成差分0）を通過。`release` スキルでマージ・push・本番確認まで完了
（`c9d7971`、`46da705..c9d7971`）。部活動ページの表示は993件→1139件、トップページも同数へ更新。
**公開ページの構造・文言は変えていない（数字表示のみ最新化）**。

**段階4（2026-09-02 完了）**: 部活動の「沈んだ大陸」4件を抽出した。一次資料メモと収集済み1139件の
突き合わせで、①支援水準の自治体差（国は統一の下限を設定していない。阿賀野市は大会参加費を全額
補助）②国庫補助の対象外費目（施設整備費は補助されない）③部活動指導員の任期は1年更新・上限4回
（東京都の例）④指導員になるための資格要件（実務・指導経験6年以上）の4件を採用。各件とも収集投稿
での言及は0〜12件（母数1139件の1.05%以下）。オーナーに4件で確定するか確認し、**4件のまま採用**と
決定（資格要件の1件も残す）。データは `data/verification/bukatsu-chiiki-sunk-continents.json`、
読みやすい版は `quality/research/bukatsu-chiiki-stage4-sunk-continents-draft.md`。公開ページ・
公開JSONは未接続（段階6で `build_planet_data.py` に接続する）。専用worktree
`task/planet-stage4`（mainから分岐、未マージ）。次は段階5（地下水脈の抽出）。

**段階4・5のレビューと差し戻し（2026-09-02）**: 独立レビュー（`quality/reviews/2026-09-02-task54-stage4-5-review.md`）。
公開ページ・公開JSON・生成スクリプトには未接続のため**本番影響は無い**（370テストOK、`docs/` 差分0）。
一次資料4点はURL到達200・記載も一次資料メモと一致、母数1139・論点件数・代表投稿12件の実在とスタンスは正典と一致。

**段階5の差し戻し理由（指摘1）**: 代表投稿12件の `summary` が正典の分類器出力 `classification.summary` と
12件中11件で完全一致（残り1件も語尾を足しただけ）。投稿本文ではなくAIの分類結果を組み替えて作られており、
設計書3.3「人が一次資料を読まないと出てこない情報だけを置く」・3.3.3「編集部が読み」・
本欄段階3の「段階4・5は定義上AIに代替させない」を満たさない。`ai_assisted` と正直に記録した点は正しいが、
そのまま段階6へ進むと「`ai_assisted` を『人が確認』と表示しない」規約により海面下を空で出すしかなくなる。
12件は全件が段階3で編集部が読んだ31件の中から選ばれており、読み直しは1セッション未満で終わる。

**段階4の修正点（指摘5〜7）**: ①4件目「資格要件」の「0件」が実データと不一致（指導者の資格を論じた投稿が
5件以上ある。無いのは「東京都の経験6年以上という数値条件」だけ）。生活への影響に一次資料に無い因果の推測が
入っており、東京都限定の但し書きも欠けている ②「無い」の確認が論点バケツの中だけで行われている
（1件目の「0件」に反する投稿が全1139件では3件ある） ③`sns_count` の意味が4件でバラバラ
（1件目だけ論点バケツの件数）。根っこは3.3.2の「機械で再現できる条件」が未実装なこと。

**段階4の修正完了（2026-09-02）**: 指摘5〜7に対応。4件すべてに `match_rule`（検索式・機械一致
tweet_id・除外理由）を追加し、`sns_count` を「争点そのものへの言及件数」に4件とも統一（論点バケツの
件数は `issue_bucket` として参考値に分離）。①4件目は「資格・経験を求める意見は5件、東京都の
実務経験6年以上という数値条件そのものへの言及は0件」に書き直し、東京都限定の但し書きを追加、
因果の推測（条件の高さが担い手不足に関わるか）を削除 ②1件目は「争点そのもの」を0件のまま維持しつつ、
論点バケツ12件を`issue_bucket`に分離し、全体で見つかった非意見の関連投稿3件（徳島県・高松市・
中体連大会の事実共有）を注記に追加 ③3件目は「会計年度任用」→実際に一致する表記「会計年度」に
修正（中身・件数は変更なし）。全パターンをスクリプトで実行し `machine_hits` と完全一致することを
確認済み。読みやすい版（`quality/research/bukatsu-chiiki-stage4-sunk-continents-draft.md`）も同内容に
更新。公開ページ・公開JSONへの接続なし（`docs/` 差分0）、370テストOK。専用worktree
`task/planet-stage45-fix`（`task/planet-stage45-review` から分岐、未マージ）。

**段階5の読み直し完了（2026-09-02、指摘1〜4対応）**: 分類器の`summary`・`reason`を見ずに、代表投稿
12件を本文だけで読み直した。vein-1（指導者の待遇の低さ）は6件とも懸念に合致し変更なし。vein-2
（移行しても教員負担は実質減らない懸念）は6件中3件（教員自身の待遇・給特法改正の話）が懸念と噛み
合っていなかったため外し、本文検索で代替3件に差し替えた（`2083719199449575575`「休日指導や引率、
事故対応、保護者対応など、教員負担の本質もそのまま」／`2091827102743957633`「応急対応を誰が担うか
は地域差ある」等）。差し替え後、vein-2の論点が4論点（制度・移行プロセス／受け皿・指導者／教員の
働き方／地域格差）にまたがったため`issue_id`を`issue_ids`配列に変更し、vein-1も同じ形に揃えた
（指摘3対応）。移行支持460件を含む水脈を3論点で探したが、賛否をまたいで同じ具体的懸念を共有する
組は見つからず、3本目は作らず2本のまま採用（探索結果を`explored_but_not_adopted`に記録、指摘2
対応）。`representative_posts`から`summary`を外し`"excerpt": "not_listed"`に置き換え（指摘4対応）。
`curated_by`・`checked_by`を`editorial_review`に変更。代表投稿tweet_idが全件正典に実在することを
機械で確認済み。読みやすい記録は`quality/reviews/2026-09-02-task54-stage5-veins.md`の「読み直し」節。
公開ページ・公開JSONへの接続なし（`docs/`・`data/public/`差分0）、370テストOK。同じ専用worktree
`task/planet-stage45-fix`で実施（未マージ）。

**指摘8の対応完了（2026-09-03）**: `scripts/verify_ocean_layer.py`と`tests/test_ocean_layer.py`（14件）を新設し、
`data/verification/*-sunk-continents.json`・`*-veins.json`の組をテーマ名を決め打ちせず自動で探して検査する。
見る内容は①沈んだ大陸1テーマ4件以内 ②各件に一次資料URL・該当箇所・`sns_count`・`sns_base`・`checked_on`・
`checked_by`がある ③`match_rule.pattern`を正典（`social-samples/`、意見のみ）に対して再実行した結果が
`machine_hits`と一致する ④地下水脈1テーマ2〜4本、各側の代表投稿2件以上でtweet_idが正典に実在する
⑤`data/verification/`のファイルに`summary`/`text`（本文相当）を残さない ⑥`checked_by`が`ai_assisted`の
まま公開JSONへ接続されていない。正典が無い環境（CI・クリーンクローン）では③④の実在確認を飛ばし、
「飛ばしました」と明示するメッセージを出す（黙って通さない、実地確認済み）。実行すると
`bukatsu-chiiki: OK`（既存2ファイルは基準を満たす）。384テストOK（370＋新規14）、`docs/`・`data/public/`差分0。
専用worktree`task/task54-stage5-checks`（mainから分岐、未マージ）。

**段階6完了（2026-09-03）**: `scripts/build_planet_data.py` を海・地下水脈へ接続した。
①陸地の集計（論点別件数・スタンス内訳・強度内訳）を非公開正典 `social-samples/` の直接読みから
公開JSON（`data/public/themes/{topic}.json`）だけの読みへ切り替えた（`totals.relevant` は公開契約に
無い値のため削除し、試作HTMLの表示文言も合わせて外した）②論点ごとに `claim_verification.claims` の
`issue_ids` を突き合わせ `verdict`（`fact`/`gap`/`miss`）と一致した主張一覧を持たせた。1論点に複数主張が
付く場合の規則（`miss`＞`gap`＞`fact`の最厳判定を採用、他の主張は消さず残す）を設計書14章へ追記した
③沈んだ大陸・地下水脈は `data/verification/{topic}-sunk-continents.json` / `-veins.json` から
テーマ単位でそのまま接続し、台帳が無いテーマは空配列で出る（推測で埋めない）。
公開ページ・公開JSONへの接続はまだ無い（`quality/prototypes/` 配下の試作JSON/HTMLだけを更新）。
専用worktree `task/planet-stage6`（mainから分岐、未マージ）。

**段階6のレビューと修正（2026-09-03）**: 独立レビュー（`quality/reviews/2026-09-03-task54-stage6-review.md`、
worktree `task/planet-stage6-review`）で条件付きpass。**指摘の核心**: 入力を公開JSONへ切り替えた際に
「間違っても止まらない」経路が残った。①立場の集計だけを表示ラベルの文字列一致で行っており、
公開側で文言を言い換えると立場合計が1139→749に欠け、陸地の色が7論点中4つで変わる（実測）
②沈んだ大陸の母数が台帳に焼き込まれ、データが増えると同じページに違う母数が並ぶ
③設定ファイルに無い論点idを黙って捨て、画面の合計が合わなくなる（実測1189対1139）
④地下水脈を論点へ結んでいない ⑤この生成器を通るテストが1本も無い ⑥未知の判定語で `KeyError`。

**対応（`task/planet-stage6-fix`、`task/planet-stage6` から分岐・未マージ）**: 指摘1〜6すべてに対応。
論点も立場も**管理番号（id）だけで結ぶ**形に揃え、結べないid・合計が合わない入力では生成を止める。
指摘2はレビューの案（母数を台帳から消して生成時に公開JSONから採る）を採らず、**照合して知らせる**形に
した。理由は①`verify_ocean_layer.py` が `sns_base` を必須項目として要求している（段階5・指摘8の検査）
②`sns_count` はその母数のもとで人が本文を読んで確定した件数で、母数だけ差し替えるとどの時点でもない
比率が出る、の2点。台帳の `sns_base` は残し、生成時に現在の `opinion_count` と突き合わせて
`base_stale` を出力・警告し、独自性の検査で**公開ページの出力を止める**（`--prototype` は通す）。
地下水脈は台帳の `issue_ids` で各論点へ結んだ（指摘4）。`tests/test_planet_data.py`（14件）を新設し、
指摘1〜4・6の再発をテストで固定した（指摘5）。**部活動の生成結果は数字・面積・標高・色・判定が
修正前と完全一致**（差分は追加項目のみ）。全398テストOK（384＋14）、`verify_ocean_layer` /
`verify_claim_verdicts` / `verify_public_registry`（2モード）/ `verify_theme_page bukatsu-chiiki` OK、
2回生成の差分0、`docs/`・`data/public/` 差分0。記録は `quality/reviews/2026-09-03-task54-stage6-fix.md`。

**未着手（レビュー指摘・急ぎではない）**:
1. 公開データ契約に「照合が対象にした期間」（`covered_period`）を項目として持たせ、
   `collection_period.end` より前ならNGにする検査を追加する（部活動含む7テーマ横断の変更）
   → **暫定対応済み（2026-09-02）**: `covered_period` の追加は未着手だが、確認日 `checked_on` を
   読んだ範囲の代わりに使い、`verify_claim_verdicts.py` が**警告**として一覧を出すようにした
   （終了コードは変えない。現在6テーマが該当）。定例更新のたびに気づける状態になったので、
   残るのは正式な項目化と、下の2（読み直す範囲のオーナー判断）
2. 既存6テーマ（bike-blue-ticket / constitutional-amendment / consumption-tax-cut /
   elderly-license-revocation / fukushuto / koshitsu-tenpakai）の確認済み時点と公開母数のズレを
   どこまで読み直すか、オーナー判断が必要
3. `chutairen-entry` の出典（中体連細則PDF）が第三者サイト掲載のみ。日本中学校体育連盟の公式URLへの
   差し替えを検討
4. 主張と論点の結び付け（編集判断）と投稿の分類論点が食い違う5件（レビュー5-2）を、段階6（陸地の色塗り）前に決める
5. `verdict` と `verdict_label`（表示文言）の対応を固定するテストが無い（段階7の前に追加）
6. ~~沈んだ大陸・地下水脈を見る検査~~ → **完了（2026-09-03）**。上の「指摘8の対応完了」参照

**（作業中の記録）ステップ0〜2完了時点**: 専用worktree `task/planet-stage3`（mainから分岐）。
993意見から主張9件を洗い出し、一次資料10点に当てて判定した（`quality/research/bukatsu-chiiki-stage3-claims-draft.md`）。
判定は fact 4件 / gap 4件 / miss 1件。公開ページ・公開JSONは触っていない（`docs/` 差分0、367テストOK）。
**オーナー判断待ち1件**: miss（BC-7「運動部費用 年8万4千円・保護者65%」は公的統計に無く、民間300人のネット調査が出どころ）を載せるか外すか。
**次**: 9/3の定例更新後にステップ3（候補投稿を1件ずつ読んで `data/bukatsu-chiiki_claim_posts.json` を確定）。

**着手タイミングの元の選択肢**: 部活動と高齢者免許返納の定例収集が 2026-09-03 に来る。
段階3は2〜3セッションかかるため必ずぶつかる。

- **A（AIの推奨）**: ステップ1〜2を先に進め、ステップ3の投稿確定だけ 9/3 の定例更新後に回す。
  確定は1件ずつ読む作業なので、途中で正典が262件級で増えると読み直しになる。洗い出しと資料照合は
  投稿が増えても揺れない
- **B**: 9/3 の定例更新を先に済ませてから段階3に入る（安全だが2日止まる）
- **C**: いまの993件で確定し、9/3 の追加分は差分だけ後で読む
  （`FACT_CHECK_GUIDE.md`「データを追加したときの扱い」どおり。手戻りは小さいが二度手間）
