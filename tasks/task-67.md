# 課題67: 定期計測が「通常ポスト」を拾わず、URL付き投稿が毎回測り漏れる

**登録日**: 2026-09-12
**状態**: 未着手

## 何が起きたか

2026-09-10 22:00 に出した方式A（本文とURLを1本にまとめた通常ポスト、今週の検証「URLの有無」の比較用）が、
24時間を過ぎても `python3 scripts/x_post_views.py pending --json` に一度も上がらなかった。
そのため 2026-09-11 20:05 の定期計測（`x-daily-measure`）でも測られず、
2026-09-12 08:34 に X運用セッションが手動で計測した（31表示・約34.5時間後）。
気づかなければ、今週の検証は判定材料が片方欠けたまま週次レビューを迎えていた。

## 原因

`scripts/x_post_views.py` の `find_pending()` は、`content/x/posts.md` の `## ○○実績 YYYY-MM-DD` 見出しのうち、
次の3つしか対象にしていない。

- `リプライ実績`（表の「自リプライ表示」列）
- `論点ポスト実績`（`投稿URL:` 行と `表示回数:` 行）
- `### 会話フォロー`（**これは誤り。下の「2026-09-20追記」を見ること**）

`通常ポスト実績` は、`論点ポスト実績` と同じ `投稿URL:` + `表示回数:` の形で書いていても、見出しの名前が違うだけで対象外になる。

一方、管理ダッシュボード（`scripts/admin_dashboard/collect.py`）は「箇条書き形式（論点ポスト・通常ポスト）」として通常ポストの表示回数を読んでいる。
**表示する側は通常ポストを知っているのに、計測待ちを出す側だけが知らない**、というずれ。

## 影響範囲（2026-09-12 時点の `content/x/posts.md`）

| 見出しの種類 | 節の数 | `pending` の対象か |
|---|---|---|
| リプライ実績 | 66 | 対象 |
| 論点ポスト実績 | 21 | 対象 |
| 通常ポスト実績 | 6 | **対象外** |
| 流入投稿実績 | 3 | **対象外** |
| 引用RT実績 / 引用リポスト実績 | 3 | 対象外 |
| URL付き告知ポスト実績 / 固定ポスト差し替え実績 | 2 | 対象外 |

- 通常ポスト6節のうち、`表示回数:` 行を持つのは 2026-09-10 の方式Aだけ。
  09-04・09-01・08-29 は `計測:` 行の旧形式で記録済み、08-01・07-15 は `投稿URL:` 行も無い古い形式。
  **直すときに古い節まで計測待ちに出さないこと**（すでに測り終えている）
- URL付き流入投稿は週1〜2本出す運用なので、放置すると**毎週1〜2本ずつ測り漏れる**

## 直し方の案

1. `find_pending()` の `論点ポスト実績` の分岐を、`通常ポスト実績` と `流入投稿実績` にも適用する
   （`投稿URL:` 行と `表示回数:` 行が両方ある節だけを対象にする。旧形式は `表示回数:` 行が無いので自然に外れる）
2. `PendingPost.kind` に見出しに応じた名前（`通常ポスト` など）を入れる
3. `apply_measurements()` が同じ節の `表示回数:` 行を書き換えられることを確認する
4. `tests/test_x_post_views.py` に、未計測の `通常ポスト実績` が `pending` に出ることと、
   `表示回数:` 行の無い旧形式が出ないことのテストを足す

## 完了条件

- 上記テストが通る
- 次にURL付き通常ポストを出したとき、24時間後の定期計測で自動的に測られる（手動計測が要らない）

## 注意

- 定期計測（毎日20:05）は別セッションで走っている。直した版が main に入る前後で、同じ投稿を二重に測らないよう、
  手動計測済みの節（2026-09-10 方式A）は `表示回数:` が埋まっていることを確認してから反映する
- リプライ実績表の8列は変えない（`OPERATIONS.md` の測定欄の注意、`scripts/admin_dashboard/collect.py` が列名に依存）
- 投稿の書式を直す方向（通常ポストを `論点ポスト実績` と書く）では解決しない。見出しは投稿の種類の記録でもある


---

## 2026-09-20追記: 会話フォローも大半が対象外だった（週次レビューで発覚）

上の「原因」で `### 会話フォロー` を対象と書いたのは**誤り**。
実際には**会話フォロー見出し15本のうち12本が対象外**で、`pending` に一度も出ていない。

### 原因（特定済み・課題67本体と同じ関数）

`scripts/x_post_views.py:27-28` の2つの正規表現の作りが揃っていない。

```python
_TOP_HEADING_RE    = re.compile(r"^##\s+(.+?)\s+(\d{4}-\d{2}-\d{2})(?:（.*?）)?\s*$")
_FOLLOW_HEADING_RE = re.compile(r"^###\s+会話フォロー\s+(\d{4}-\d{2}-\d{2})\s*$")
```

`##` 側には `(?:（.*?）)?` があり `## リプライ実績 2026-09-20（昼枠）` の末尾カッコを許している。
`###` 側にはこれが無く、**日付で終わる行しか通らない。**

```
MISS  ### 会話フォロー 2026-09-20（投稿済み）
MISS  ### 会話フォロー 2026-09-18（投稿済み）
MATCH ### 会話フォロー 2026-09-15          ← カッコ無しなら通る
```

`pending` は該当なしのとき空配列を返すため、**画面上は「もれ無し」に見える。**
2週連続で気づかれなかったのはこのため。

### 実害（2026-09-20時点の実測）

`自リプライURL:` はあるが `表示回数:` が無い＝**投稿したのに未計測**の会話フォローが5本。約2週間分。

| 日付 | 見出し |
|---|---|
| 2026-09-09 | ### 会話フォロー 2026-09-09（投稿済み） |
| 2026-09-11 | ### 会話フォロー 2026-09-11（投稿済み） |
| 2026-09-15 | ### 会話フォロー 2026-09-15（投稿済み） |
| 2026-09-18 | ### 会話フォロー 2026-09-18（投稿済み） |
| 2026-09-20 | ### 会話フォロー 2026-09-20（投稿済み） |

会話フォローは「返信が付いた投稿」という数少ない成功例への追撃なので、
ここが測れないと**何が会話につながったのかを週次レビューで評価できない。**

### 追加でやること

1. `_FOLLOW_HEADING_RE` に `(?:（.*?）)?` を足し、`##` 側と同じ形に揃える
2. **投稿していない**会話フォロー（`（送信なし）` `（返信案のみ・未送信）`）を `pending` に出さない。
   判定は見出しの文字列ではなく **`自リプライURL:` 行の有無**で行う
   （見出しの言い回しは今後も増える。増えても壊れない判定にする）
3. **未知の見出しを黙って捨てない。** 既知の種別リストに無い `## ◯◯実績 YYYY-MM-DD` を見つけたら
   警告を出すか `pending` に載せる。ここを直さないと4回目が起きる
   （1回目=通常ポスト実績、2回目=流入投稿実績、3回目=会話フォロー。いずれも同じ構造）
4. 上記5本を遡って計測し、`content/x/posts.md` に `表示回数:` を追記する
   （投稿から日が経っているため、計測時点の経過時間を注記する）

### 追加の完了条件

- `### 会話フォロー YYYY-MM-DD（任意の接尾辞）` が検出されるテスト
- `自リプライURL:` の無い会話フォローが `pending` に出ないテスト
- 既知リストに無い `## ◯◯実績 YYYY-MM-DD` が黙殺されないテスト
- 上記5本の遡り計測が完了している

### 関連

- `content/x/weekly-reviews.md` 2026-09-14〜2026-09-20 の節（この追記のきっかけ）
- `content/x/weekly-reviews.md` 2026-09-07〜2026-09-13 の節（前回の指摘。未対応のまま）
