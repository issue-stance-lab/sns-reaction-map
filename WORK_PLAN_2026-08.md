# WORK_PLAN 2026-08 — 信頼性の回復と流入の立ち上げ

作成: 2026-08-01 / 対象期間: 2026-08-01 〜 2026-08-28（4週間）
関連: `TASK_BOARD.md`（課題15・18・19）/ `GROWTH.yaml` / `LOOP.md`

---

## 0. この計画の前提

Gemini・Codex・Claude の3者で議論した結論を1本の工程に統合したもの。

**現状認識**

| 指標 | 値 | 出典 |
|---|---|---|
| 週間実ユーザー | 10 | GROWTH.yaml 2026-07-26 |
| 総投票数 | 12 | 同上 |
| GSC 表示回数（28日） | 45 | 同上 |
| GSC クリック（28日） | 2 | 同上 |
| 公開テーマ数 | 11 | THEMES.yaml |
| AdSense | 不承認（有用性の低いコンテンツ） | TASK_BOARD 課題15 |

**2つの問題を同時に抱えている**

1. **信頼性** — トップページに、どのデータからも計算されていない数字が並んでいる
2. **流入** — 週10ユーザーでは、何を改善しても効果を測定できない

この2つは**直列にしてはいけない**。1を3日で直したあと「1週間観察」しても、週10ユーザーからは何の情報も得られない。したがって本計画は**2トラック並行**で組む。

- **トラックA（信頼性）**: 掲載している数字の裏を取る。主にAdSense再申請と再訪率のための投資
- **トラックB（流入）**: 見に来る人を増やす。初回流入はAの改善では増えない

**この期間やらないこと**

- 新テーマの追加（LOOP.md ②の優先順位5は当面凍結）
- 新しい可視化・大型機能の開発
- 母数不足でのA/Bテストと効果判定

---

## 1. Week 0 完了分（記録）

### ✅ 0-1. トップページ上書き事故の防止

- `run_pipeline.py` から `step_portal` の呼び出しを削除、`--skip-portal` は非推奨警告に降格
- `build_site_portal.py` は `--output` を必須化し、`docs/index.html` を指定すると `parser.error` で停止
- `tests/test_portal_overwrite_safety.py` で index.html の sha256 を dry-run 前後比較する回帰テストを追加

**残作業**: 上記は**すべて未コミット**。トラックAに着手する前に `task/pipeline-safety` ブランチでコミット・PR・マージすること。事故防止の変更とトップ改造が同じ差分に混ざると切り戻し不能になる。

---

## 2. 着手前に確定させること（半日・最優先）

トップページの改修に入る前に、**「分析済み投稿数」の正典を決める**必要がある。これは当初「1時間で直せる」と見積もったが、調査の結果もっと根が深い。

### 判明した事実

`sync_portal_stats.py` の `parse_themes_yaml()` は THEMES.yaml の**コメント**を正規表現で拾っている。

```python
c2d_match = re.search(r"classify2d:\s*done\s*#\s*(\d+)件", block)
```

実際に走らせた結果：

| | 値 |
|---|---|
| トップページの表示値 | **5,953** |
| スクリプトが計算する値 | **2,946** |
| 差 | 3,007（表示値はスクリプト値の約2倍） |

さらに、公開中11テーマのうち**2テーマが count=0 になっている**。

- `bukatsu-chiiki`: コメントが `# 旧2D 245件は履歴保持。...` で、`# ` の直後が数字でないため正規表現が外れる
- `consumption-tax-cut`: `classify2d: n-a`（Hermes論点分類で直接カバー）のため対象外になる

つまり **今このスクリプトを実行すると、見出しの数字が 5,953 → 2,946 に半減する**。そして 5,953 は過去に手で書き換えられた値（git log: `fix(hero): 分析済み投稿件数を3191→3684に修正`）で、どちらも実データと突き合わせて検証されていない。

加えて `social-samples/` には1テーマあたり複数の候補ファイルが存在する。

```
ai-copyright_classified.json        475件
ai-copyright_2d_classified.json     708件
ai-copyright-v2_samples_refresh...  458件
```

**どれを「分析済み投稿」と呼ぶかが決まっていない。** これが数字が腐り続けた根本原因であり、フィールドを1つ足せば済む話ではない。

### 0-2. 件数の正典を定義する

**作業**

1. 各テーマについて「公開ページがどのJSONから生成されているか」を確定する（`run_pipeline.py` / 各テーマの生成コマンドを追跡）
2. THEMES.yaml に**明示フィールド**を追加する。コメントからの抽出は廃止。

```yaml
  ai-copyright:
    title: 生成AIと著作権
    sample_file: social-samples/ai-copyright_2d_classified.json   # 公開ページの生成元
    sample_period: "2026-06-10〜2026-07-26"                        # 取得期間
    sample_source: "Yahooリアルタイム検索"
```

3. `sync_portal_stats.py` を書き換え、**件数は `sample_file` を実際に開いて数える**（コメントも手入力フィールドも信用しない）
4. `classify2d` コメントからの抽出コードを削除
5. 用語を実態に合わせる。現在の「分析済み投稿」が数えているのは分類済みの有効件数であって収集件数ではない → **「分類済み投稿」**に統一

**完了条件**

- [ ] 11テーマすべてに `sample_file` / `sample_period` / `sample_source` がある
- [ ] `sync_portal_stats.py` が全テーマで count>0 を返す（0件テーマがあれば異常終了する）
- [ ] トップの表示値がスクリプト実行結果と一致する
- [ ] 数字の根拠を「このファイルのレコード数」と1行で説明できる

> この作業は Week 1 のトップ改修の**前提**。ここが決まらないうちは、トップに件数を出さない。

---

## 3. トラックA：信頼性（Week 1〜2）

### A-1. トップの根拠不明表示を差し替える（1日）

**削除**

- 公開テーマの分類比率ドーナツ（38/33/17/12% — 由来なし）
- 最近の分類件数グラフ（7/7〜7/12 で固定。8月になっても7月の日付）
- 割れ度ランキング（78/76/74/72 — 算出ロジックが存在しない）
- ランキング用の各テーマ割合（29/26/35/10% 等 — 手入力）
- ナビゲーションの「ランキング」項目と `#ranking` アンカー

**追加（同一PRで実施。空白を残さない）**

| 位置 | 差し替え後 |
|---|---|
| ドーナツ枠 | 「いま考えたい問い」 |
| 推移グラフ枠 | 「このサイトの読み方」 |
| 割れ度4カード | 「いま考えたい4つの問い」 |
| 中央マップ | 「論点整理のイメージ」ラベルを明示 |
| ナビ | ランキング → 注目の問い |

**掲載する4つの問い**

1. AI学習が合法なら、創作者への対価は不要なのか？ → `ai-copyright`
2. 自転車の取り締まりと道路整備、どちらが先か？ → `bike-blue-ticket`
3. 教員の負担を減らして、家庭の負担が増えてもよいか？ → `bukatsu-chiiki`
4. 減税で浮いた分を、10年後の自分の年金から出してもよいか？ → `consumption-tax-cut`

> 4番は当初案「家計支援と社会保障財源をどう両立するか？」から変更。元の文は政策文書の見出しであって、読者が自分の立場を試される問いになっていないため。

**選定基準を GROWTH.yaml に記録すること。** 基準のない4本の選抜は、割れ度ランキングと同じ「説明できない表示」に将来なる。

**完了条件**

- [ ] 順位・割合・割れ度がページ内に存在しない（grep で確認）
- [ ] PC で現在と同程度の視覚密度がある（スカスカにしない）
- [ ] 4カードすべてから対応テーマページへ遷移できる
- [ ] `#ranking` を参照している他ページのリンクが残っていない

### A-2. ヒーロー統計を整理する（半日）

**残す**

- 公開テーマ数（THEMES.yaml から生成）
- 分類済み投稿数（0-2 完了後に復活。`sample_file` の実レコード数）
- 最終データ更新日

**次回データ更新日は「削除」ではなく「修復」する**

当初案は削除だったが、日付が過ぎたときの分岐は `docs/index.html:254` に**すでに実装済み**で壊れていない。

```js
var next=new Date('2026-08-02T00:00:00+09:00'),now=new Date(),
days=Math.ceil((next-now)/864e5),
txt=days>0?'あと'+days+'日':days===0?'本日更新予定':'更新済み';
```

壊れているのは別の点：

- 日付 `2026-08-02` が**2箇所にハードコード**（`<strong id="hero-next-update">` の文字列と上記JSリテラル）
- `sync_portal_stats.py` はそのどちらも更新しない
- **THEMES.yaml に `refresh_at: 2026-08-02` という真実源がすでにある**

→ `refresh_at` の最も早い日付から2箇所を生成する。20行程度。

「毎週、観測条件を揃えて更新される」というポジショニングを掲げる以上、次回更新日は消すべきでない表示である。

**完了条件**

- [ ] 表示中のすべての数字と日付が THEMES.yaml から再生成できる
- [ ] ハードコードされた日付がページ内に存在しない
- [ ] 更新を忘れても虚偽表示にならない（過ぎた日付は「更新済み」表示になる）

### A-3. レスポンシブ対応（半日）

PC（1280px）: 左=コピー / 中央=論点整理イメージ / 右=注目の問い＋読み方 の3列を維持
スマホ（390px）: 中央マップ → 注目の問い → このサイトの読み方 → テーマを見るボタン の縦積み

**完了条件**

- [ ] 1280px と 390px で不自然な空白がない
- [ ] 横スクロールが発生しない
- [ ] 問いカードがスマホで非表示にならない（現在 `.topics-toolbar` 等が `display:none` になる箇所あり。同じ轍を踏まない）
- [ ] 文字が画像・カードからはみ出さない

### A-4. 3種類の数字を分離する（半日）

同じ色・同じグラフ・同じ見出しで混ぜない。

| 種別 | 表示ルール |
|---|---|
| SNS収集サンプル | 割合より件数を主表示。近くに取得元・期間・検索語・重複除去後件数・AI分類方法・人間確認範囲 |
| サイト参加者投票 | 常に「参加者投票 n=○○」を併記 |
| 世論調査 | **実施していない**ことを明示 |

注意書きは**ページ末尾ではなく、最初の数字の直前**に置く。

```
掲載内容はSNS公開投稿のサンプルを整理したものであり、
社会全体の世論調査ではありません。
```

「このサイトの読み方」カードから `about.html` へリンクする。

### A-5. 自動検査を追加する（半日）

`tests/` に追加し、公開前チェックに組み込む。

**出力の検査**

- [ ] 「割れ度」が存在しない
- [ ] 「公開テーマの分類比率」が存在しない
- [ ] 30日以上前の日付を含む推移グラフが存在しない
- [ ] 4つの問いカードのリンクがすべて有効
- [ ] 公開テーマ数が THEMES.yaml と一致する
- [ ] パイプライン実行後もトップが維持される（0-1 のテストを流用）

**⚠️ 最重要: 正規表現の空振り検出**

`sync_portal_stats.py` には**マッチせず、エラーも出さずに成功する置換**が現存する。

```python
r'(<em>)\d+/\d+更新 \+[\d,]+件追加(</em>)'   # 期待: "7/29更新 +452件追加"
# 実際のHTML: <em>7/29更新</em>  → マッチせず、黙って何もしない
```

この結果、update-bar は「最終更新: 2026年7月26日」、hero は「7/29更新」で食い違ったまま放置されている。

→ **`update_html()` の各置換が最低1件マッチしたことを検証し、0件なら異常終了する**処理を必ず入れる。禁止語のチェックだけでは、この「静かに何もしない」バグは永久に検出できない。トップページの数字が腐った根本原因はここにある。

### A-6. AdSense 再申請（Week 2 末）

A-1〜A-5 完了後に実施。不承認理由「有用性の低いコンテンツ」に対する直接的な回答が、A-1（根拠不明な数字の削除）と B-2（両側の論拠）である。

収益の話であると同時に、**第三者による品質の外部検証**として機能する。

- [ ] TASK_BOARD 課題15 の残作業①（git push で公開）を先に完了
- [ ] AdSense 管理画面から「審査をリクエスト」
- [ ] 結果を TASK_BOARD 課題15 に記録

---

## 4. トラックB：流入（Week 1 から並行開始）

**トラックAの完了を待たない。** 信頼性の改善が効くのは「来た人が再訪するか」「AdSenseが通るか」であって、初回流入には効かない。

### B-1. 現状把握（Week 1・計1時間）

いま何も測れていない項目を埋める。判断の前提になるので最初にやる。

- [ ] **GSC のクエリ別・ページ別データを取得**（15分）
  - GROWTH.yaml にあるのは全体値（表示45・クリック2・平均掲載順位11.51）のみ
  - どのページのどの語で表示が出ているかが不明なまま代表テーマを選ぶのは、根拠のない数字を消す計画としては一貫性を欠く
  - **結果次第で B-2 の対象テーマが変わる**
- [ ] **X のフォロワー実数を確認**（5分）— GROWTH.yaml で `x_followers: null` のまま。流入を賭ける経路の規模を把握していない
- [ ] 取得値を GROWTH.yaml の kpi スナップショットに追記

### B-2. 代表テーマを1本、完成形にする（Week 2〜3）

対象は B-1 の結果で決定。現時点の候補は `ai-copyright`（GSC で表示が出ている可能性が最も高い）。

**完成条件**

- [ ] 検索意図に合うタイトル
- [ ] 30秒要約
- [ ] 争点別の解説
- [ ] **両側の最も強い論拠**（下記）
- [ ] 一次情報・公的資料へのリンク
- [ ] SNSサンプルの調査条件（取得元・期間・検索語・件数・分類方法）
- [ ] 投票
- [ ] 関連テーマ導線

**「反対側の、いちばん強い論拠」の構成**

1. 立場Aの最も強い論拠
2. 立場Bの最も強い論拠
3. 両者が共有している前提
4. 本当に対立している点
5. まだ確認できていない点
6. 関連する一次情報

選定基準も公開する：具体的な根拠がある／人物攻撃ではない／検証可能／相手の立場を単純化していない／高リスク未確認情報でない。

「代表的なSNS投稿」より**上**に配置する。これがこのサイトの差別化の核。

> **⚠️ スコープ注意**: `configs/*.json` に該当フィールドは存在しない（`background` は別物）。実装は「スキーマ追加 → `build_reaction_map.py` 対応 → 執筆」の3段階。執筆は自動化できない。
> **Week 2〜3 では1テーマのみ実施し、ここでスキーマを確定させる。** 11テーマ×5要素=55本を同時に走らせない。横展開は Week 4 以降の判断事項。

### B-3. X 運用を「割合」から「問い」へ（Week 1 から継続）

避ける投稿：

> 規制賛成が60%でした。

推奨する投稿：

> 「AI学習は合法」でも、クリエイターが納得できないのはなぜか。
> SNS上の議論を整理すると、著作権だけでなく「対価」「表示」「仕事への影響」の3論点が重なっていました。

数字の驚きではなく、読者が考えたくなる未解決の問いを入口にする。

### B-4. X 以外の入口を1つ試す（Week 3）

X のフォロワーが二桁の段階で X だけに賭けるのは危うい。**ドメイン評価に依存しない経路**を1つ試す。

- はてなブックマーク／ニュース系コミュニティへの投稿
- 対象は B-2 で完成させた1ページ
- 反応の有無を GROWTH.yaml の activity_log に記録

### B-5. 独自ドメイン移行の判断（Week 3〜4）

TASK_BOARD 課題15 に「github.io サブドメインは審査上不利」「長期的に検討」と既出。これは AdSense だけの問題ではなく**検索評価にも効く**。

移行しないまま B-2 の効果を測ると、「コンテンツが弱いのか、ドメインが弱いのか」が永久に切り分けられない。

- [ ] ドメイン取得費用と移行手順を洗い出す
- [ ] AdSense 再申請の結果を見てから最終判断（不承認が続く場合は移行を優先）

---

## 5. 指標の扱い

### 主要KPI（この2つだけ）

- 週間実ユーザー（GA4・公開ホスト限定）
- GSC 表示回数

### 診断イベント（KPIではない）

母数10では優劣を判定できない。**「発火しているか」だけを確認する。**

- 注目の問いカードのクリック
- トップからテーマページへの遷移
- データ説明ページへの遷移
- 反対側の論拠を開いた

### GROWTH.yaml の整理（Week 1）

現在 capabilities が**5件同時に measuring**、全件が judge_at 延長済み、全件の reflection が「n=10で判定不能」。計測しているフリのコストだけを払っている状態。

- [ ] 安全に動作している共通機能 → `done`
- [ ] 不具合のある機能 → `building`
- [ ] 本当に検証したい施策 → `experiments` に**1件だけ**
- [ ] X投稿・データ更新 → `recurring`
- [ ] **母数不足による judge_at 延長を今後行わない**旨をルールに明記

---

## 6. 実施順とPR分割

### Week 1

| # | 作業 | ブランチ | 見積 |
|---|---|---|---|
| 1 | Week 0 の事故防止をコミット・マージ | `task/pipeline-safety` | 0.5h |
| 2 | 件数の正典を定義（§2） | `task/sample-count-canonical` | 0.5日 |
| 3 | GSC・X の現状把握（B-1） | — | 1h |
| 4 | トップの問いカード差し替え（A-1） | `task/homepage-question-redesign` | 1日 |
| 5 | ヒーロー統計整理・レスポンシブ（A-2/A-3） | `task/homepage-trust` | 1日 |
| 6 | GROWTH.yaml 整理 | 同上 | 0.5h |
| 7 | 8/2 のデータ更新を予定通り実行 | — | — |

> 8/2 は `refresh_at` の期日。新しい生成経路が本番で動くかを検証する最初の機会になるので、後ろ倒しにしない。

### Week 2

| # | 作業 | ブランチ | 見積 |
|---|---|---|---|
| 8 | 数字の3分離・注意書き（A-4） | `task/homepage-trust` | 0.5日 |
| 9 | 自動検査（A-5、正規表現空振り検出を含む） | `task/portal-validation` | 0.5日 |
| 10 | 代表テーマの「両側の論拠」スキーマ設計＋執筆開始（B-2） | `task/{theme}-arguments` | 2日 |
| 11 | AdSense 再申請（A-6） | — | 0.5h |

### Week 3

- 代表テーマ1本を完成形へ（B-2 完了）
- X 以外の入口を1つ試す（B-4）
- 独自ドメインの費用・手順を洗い出す（B-5）

### Week 4

- 主要KPI 2項目の推移を確認
- AdSense 結果を TASK_BOARD に記録
- 代表テーマの結果を見て**横展開の可否を判断**
- 週間ユーザー100人に到達するまで、新しい大型機能は追加しない（GROWTH.yaml phase.until と整合）

---

## 7. この計画の失敗条件

以下に該当したら、計画を続けるのではなく立ち止まって見直す。

- Week 2 末までにトラックB が1つも着手されていない（＝また直列に戻っている）
- 母数不足を理由に judge_at を延長する記述が GROWTH.yaml に再び現れる
- 「両側の論拠」を1テーマで確定させる前に横展開を始めている
- トップページに、生成元を1行で説明できない数字が再び現れる

---

## 8. 一言でいうと

トップページは3日で直る。問題は、直したページを見に来る経路が今はどこにもないこと。
**サイトを綺麗にする作業に安心して長居しないこと**が、この4週間で最も注意すべき点である。
