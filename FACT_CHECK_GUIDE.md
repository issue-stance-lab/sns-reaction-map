# FACT_CHECK_GUIDE.md — 投稿の主張を一次資料と突き合わせる工程を、既存テーマへ広げる

投稿が事実として言っていることを一次資料と突き合わせ、**その過程をページに載せる**ための手順。
分類でも論点整理でもなく、「調べた記録」を作るのが目的。

**なぜ要るか。** AdSenseの診断（2026-08-16 セオリコ）で、12項目のうち
**Experience（体験・経験）だけがNG**だった。指摘の原文は「著者が実際に経験した取材、
調査、検証の過程が本文に見当たらず、SNS投稿の二次整理に留まっている」。
専門性・コンテンツ品質・信頼性はすでにOK判定なので、**解説を厚くするのではなく、
誰が・いつ・何を確かめたかを残す。** 分類結果をきれいに見せる工夫（グラフ・内訳・
アニメーション）は、いくら足しても反証にならない。

同じ診断で「自動生成コンテンツの疑い」はOKだった。根拠は「定型的な繰り返しがなく、
論点ごとに異なる構造」。**この判定を壊さないことが、2本目以降のいちばん重い制約になる。**

### 先行事例の読む順番（ここを間違えると必ずテンプレになる）

先行事例は `bike-blue-ticket`。ただし**読むのは、自分のテーマの文章を書き終えてから。**

2026-08-18、この手順書に「見出しをコピーするな」と書いたうえで高齢者免許返納を作ったが、
`STEP 2 — 確かめ方` という見出しが完全一致し、書き出しも7割一致した。原因は、同じ手順書が
「先行事例を必ず先に読め・雛形にせよ」と指示していたこと。**目の前の完成品は、禁止の一文より強い。**

1. 先に、自分のテーマの投稿と一次資料だけを見て、**見出し・書き出し・判定の言い回しを自分で書く**
2. そのあとで bike を開く。見るのは**スクリプトの作り方とデータの形だけ**

- `scripts/build_bike_process_sections.py` — 生成スクリプトの作り（`write_provenance_records()` を必ず見る）
- `data/bike-blue-ticket_claim_posts.json` — 主張ごとの該当投稿IDの形
- `configs/bike-blue-ticket-reaction-map.json` の `number_provenance` — 数字の出所の登録の仕方
- `THEMES.yaml` の `bike-blue-ticket` の `notes` に経緯

## 対象テーマの選び方

一次資料で確かめられる主張が多いテーマほど成果が出る。投稿に**数・金額・年齢・時期・
制度の適用範囲・統計の増減**が出ているかで判断する。「減税したらどうなるか」のような
将来予測が争点の中心にあるテーマは、検証できる主張が現状の数字に限られるため後回しでよい。

## 発注文（このまま新しいセッションに貼る）

`{テーマslug}` を差し替えて使う。

```text
このリポジトリの既存テーマ「{テーマslug}」のページに、投稿が事実として述べている主張を
一次資料と突き合わせるセクションを追加してください。**セクション名は自分で決めること。**
先行事例（bike-blue-ticket）は実装済みですが、開くのは自分のテーマの文章を書き終えてからです。
理由は「■ 他ページと同じ形にしないこと」に書いてあります。

■ 何をするか
投稿が事実として言っていることを一次資料と突き合わせ、その過程をページに載せます。
分類や論点整理ではありません。「調べた記録」を作るのが目的です。
AdSense審査で、12項目中 Experience（体験・経験）だけがNGでした。指摘は
「著者が実際に経験した取材・調査・検証の過程が本文に見当たらない」。その反証にあたる
部分です。専門性や品質はすでにOK判定なので、解説を厚くするのではなく、
誰が・いつ・何を確かめたかを残してください。

■ 着手前
1. CLAUDE.md / README.md / THEMES.yaml を読む。オーナーは非エンジニアなので
   説明はCLAUDE.md末尾の「オーナーへの説明のしかた」に従うこと。
2. OPERATIONS.md ⓪ に従い専用の作業ツリーを作る。共有ツリーで作業しない。
     git worktree add ../isa-wt-{作業名} -b task/{作業名}
   作る前に共有ツリーの main が origin/main と同じか確認する。古いと分岐元が狂う。
3. 正典データを作業ツリーに揃える（無いと検査が「ファイルがない」で落ちる）:
     rsync -a --ignore-existing ../issue-stance-aggregator/social-samples/ social-samples/
     rsync -a --ignore-existing ../issue-stance-aggregator/data/verification/ data/verification/
4. 着手前に検査を1回流し、失敗数を記録する（完了時にこの数と比べる）:
     python3 -m unittest discover -s tests 2>&1 | tail -3
5. 先行事例（bike-blue-ticket）は、**自分のテーマの文章を書き終えてから**開くこと。
   見るのはスクリプトの作り方とデータの形だけで、文章は見本にしない。
     scripts/build_bike_process_sections.py（特に write_provenance_records()）
     data/bike-blue-ticket_claim_posts.json
     configs/bike-blue-ticket-reaction-map.json の number_provenance
   THEMES.yaml の bike-blue-ticket の notes に経緯が全部書いてある。

■ 手順
1. 対象テーマの分類済みデータ（THEMES.yaml の sample_file）を読み、
   「事実として確かめられる主張」を洗い出す。意見や価値判断は対象外。
   対象になるのは、数・金額・年齢・時期・制度の適用範囲・統計の増減など、
   公的資料で真偽や幅を確認できるもの。
2. 主張ごとに候補投稿をキーワードで集める。ただし件数にしてはいけない。
   必ず候補を1件ずつ読み、実際にその主張をしている投稿だけを残す。
   （bikeでは機械抽出のままだと実際より3〜4割多く出た。無関係な金額や
   報道の共有まで拾うため。逆に正規表現の文字数制限で本命を取りこぼしてもいた）
3. 残った投稿の tweet_id を data/{テーマslug}_claim_posts.json に保存する。
   以後の件数は必ずこのIDから数える。フォーマットは bike のものに合わせる。
4. 主張は3〜7つに絞る。目安として同じ主張が5件以上あるものを優先する。
   ただし件数が少なくても、投稿が前提にしている数字（統計・種類数・金額・年齢）は
   優先して確かめること。bike では2件しかない主張から食い違いと確認できずが出ており、
   その2つがいちばん効いている。**主張の数を先行テーマと揃えないこと。**
5. 一次資料に当たる。省庁・自治体・裁判所・国会・公的統計のみ。
   報道・まとめ・解説サイトは不可。WebFetchで原文を取得し、原文の表現を残して引用する。
   PDFしかない場合は pdftotext -layout でテキスト化して数える。
6. 判定は3種類だけ。
     fact = 資料と一致する
     gap  = 食い違う（数字は合うが幅が違う、前提が成立しない、を含む）
     miss = 確認できなかった
   miss の行を消さないこと。空振りが残っていることが人の作業の証拠になる。
   miss は「投稿が誤り」と書かない。確認できなかった、とだけ書く。
   判定の呼び名（「一致する」「食い違う」など）は、他テーマと同じ言い方にしない。
7. miss が出たら、その時点でオーナーに報告し、載せるか外すかの判断を仰ぐ。
   判断を待つ間に他の作業を進めてよいが、勝手に消さない。
   miss が1つも出なかった場合は、調べ方が浅い疑いがある。もう一度、投稿が前提にしている
   数字を疑うこと。
8. 確認日をページに明記する。
9. 生成スクリプトを scripts/build_{テーマ}_process_sections.py として作る。
   HTMLにマーカーコメントを置き、その間だけを差し替える方式にする。
   件数はデータから毎回数え直す。生成後の自己検証を必ず入れる。
   ただし新規に作る前に、既存の生成スクリプトを共有できないかを先に検討し、
   できない理由があるときだけ新設して、その理由を THEMES.yaml の notes に書く。

■ 他ページと同じ形にしないこと（検査で落ちます）
`python3 scripts/verify_page_originality.py` が、ページ間で同じ文・似すぎた見出しを
見つけて止める。しきい値は類似度0.70。**通らないと公開できない。**
- 見出しと書き出しを先行テーマからコピーしない（「STEP 2 — 確かめ方」も使わない）
- 判定ラベルの言い方を変える。3分類という設計は残してよい
- セクションの位置と順番も先行テーマと同じにしない。確かめ方だけを1セクション置く形でよい
- 共通化してよいのは仕組み（スクリプト・データ形式・検査）だけ。読者に見える文は共通化しない
- 共通で当たり前の文（投票の操作説明など）は configs/page-originality.json に
  理由つきで登録する。**編集で書いた本文をここに逃がさないこと。検査の意味が無くなる**

■ やってはいけないこと
- bike の STEP1（集め方）と STEP3（分かったこと）をそのままコピーしない。
  全テーマ共通の見出しや順番を作ることは編集方針で禁止されている。
- 一次資料が乏しく、確かめられる主張が3つも集まらない場合は、無理に作らない。
  その旨をオーナーに報告して止める。
- 一般論や記憶で事実を補わない。資料に書いていないことは書かない。
- 賛否の要約を冒頭に置かない。
- 確認できたものだけ並べない。空振りが1つも出なかった場合は、調べ方が浅い疑いがある。
  もう一度、投稿が前提にしている数字を疑うこと。
- AIの分類理由（reason）を全件並べない。機械の作業ログを見せることになり逆効果。

■ このリポジトリ固有の落とし穴（実際に踏んだもの）
- ページに出す N件 は、すべて出所を登録しないと verify_number_provenance.py で落ちる
  （2026-08-17 に実際に落ちた）。検査はレコードの配列しか出所にできないので、
  編集部が手で割り当てた件数は、1行1投稿の配列として
  data/verification/{テーマslug}-claims.json に書き出し、
  configs/{テーマslug}-reaction-map.json の number_provenance.sources に
  selectors つきで登録する。bike の write_provenance_records() が雛形。
  一次資料から引いた数字（統計値など）は allow に value と reason の両方を書く。
- 公開されるのは docs/ 配下だけ。本文に data/… のパスを書くと、読者が開けない
  ファイルを案内することになる（2026-08-18 に高齢者ページで発生）。
- 共通CSSに td { white-space: nowrap } があり、新しい表の本文が1行に潰れる。
  必ず white-space: normal で上書きすること。
- 共通CSSは !important を多用している。上書きが効かないときは特異度ではなく
  !important を疑う。
- <span class="conclusion-count"> はページに1つだけ必要。scripts/sync_issue_counts.py が
  これを書き換えるので、消すとテストが落ちる。移動させるのは可。
- 投稿の本文を載せるのは classification.article_usable が true かつ risk が low のものだけ。
  それ以外は抜粋を出さず、リンクのみにして、その判断をページに明記する。
- 一部テーマは正典データが gitignore 対象で、新しい作業ツリーには入らない。
  検査が「ファイルがない」で落ちたらこれ。対象テーマのデータが入っているか先に確認する。

■ 通すべき検査
  python3 scripts/verify_theme_page.py {テーマslug}          # NGゼロにする
  python3 scripts/verify_number_provenance.py {テーマslug}   # 説明できない数字ゼロ
  python3 scripts/verify_page_originality.py                 # 言い回しの使い回しゼロ
  python3 scripts/seo/strip_production_notes.py --check      # 制作指示の混入
  python3 scripts/seo/add_embed_fallback.py --check          # 空の埋め込み
  python3 scripts/seo/unify_site_chrome.py --check           # フッター統一リンク
  python3 -m unittest discover -s tests                      # 着手前の失敗数と比較する
  生成スクリプトを2回続けて実行し、差分がゼロであること
ブラウザでの確認も必須。PCとスマホ幅の両方で横スクロールが出ないこと、
コンソールエラーが出ないこと。
- 落とし穴: プレビュー画面が隠れているとアニメーションが停止し、要素が opacity:0 の
  まま観測される。表示不具合と誤判定しないこと。getAnimations() の currentTime が 0 なら、
  ページの不具合ではなくプレビュー側の停止。
- 表示が確認できない場合は、その旨をはっきり報告し、「確認した」と書かないこと。

■ 報告のしかた
結論を先に書く。コマンドを渡すときは「何をするか・コピペで動く形・どう見えたら成功か」の
3点セットにする。できなかったこと・確認していないことははっきり書く。
最後は次にやることを1つだけ示して終える。

■ 完了条件
- 確かめ方のセクションが対象テーマのページに入っている
- 見出し・書き出し・判定の言い回しが、他のどのページとも別の文になっている
- 該当投稿IDが data/ に保存され、件数がそこから数えられている
- miss（確認できず）の扱いがオーナーの判断を仰いだうえで決まっている
- 検査が全部通り、生成スクリプトが再実行可能
- THEMES.yaml の notes に経緯を追記してある
実装後、main へのマージはオーナーが行う。マージコマンドを渡して終えること。
```

## bike-blue-ticket での結果（2026-08-16 確認）

7つの主張を突き合わせ、**4つで食い違い、1つは確認できず**だった。

| 投稿の主張 | 該当 | 結果 |
|---|---|---|
| LUUPは免許不要で野放しなのに自転車だけ青切符 | 10件 | 食い違う（特定小型原付も青切符の対象） |
| 反則金は6,000円くらい | 8件 | 一致（信号無視6,000円・ながらスマホ12,000円） |
| 歩道を少し走っただけで即青切符 | 7件 | 食い違う（単なる歩道通行は原則指導警告） |
| 対象は16歳以上 | 5件 | 正しい |
| 取締りなんてされていない | 3件 | 食い違う（警告110万件・検挙6万件） |
| 事故は減っている／増えている | 2件 | どちらとも合わない（前年比61件減＝0.09%） |
| 違反は113種類ある | 2件 | **確認できず**（公表一覧は65項目。別の数え方の可能性を否定できないため両論併記） |

**件数は当初、機械抽出のまま14件・11件などと表示していた。**読み直すと、ポイ捨ての過料や
ヘルメット購入補助まで拾っており、3〜4割の水増しだった。事実確認を掲げたページの数字が
未確認、という最悪の組み合わせになるので、必ず1件ずつ読んで確定すること。

## データを追加したときの扱い

- 件数はスクリプトが毎回数え直すので、本文の数字は触らなくてよい
- 主張ごとの該当投稿は増えるので、新規投稿ぶんだけ読んで `data/{テーマ}_claim_posts.json` に追記する
- 同じ主張が5件を超えたら、新しいカードとして追加する
- 一次資料は改訂される。**確認日を見て、半年を目安に再確認する。**リンク切れも確認する

## 一度に何本まで広げるか

**2〜3テーマずつ。**bike 1本で「自動生成の疑い」がOKだったからといって、11本並べて
通るとは限らない。2〜3本作って公開し、セオリコで再診断（代表記事は新しく作ったテーマに
変える）してから残りに広げる。11本作ってから崩れると、直す対象も11本になる。

一次資料が乏しいテーマ（世論の受け止めが中心で、確かめられる数字が少ない）は、
無理にこの形にしない。全テーマを同じ形にしないこと自体が、審査対策としても正しい側にある。

## 2026-08-18 に起きたこと（2本目の記録）

高齢者免許返納に追加した際、中身（7主張・該当32件を1件ずつ確定・空振り1件を保持）は
手順どおりだったが、**見出しと書き出しが自転車の複製になっていた**。手順書の禁止では
防げなかったため、`scripts/verify_page_originality.py` を作って検査で止める形にした。
既存11ページから採取した共通文41件を `configs/page-originality.json` に登録してある。
