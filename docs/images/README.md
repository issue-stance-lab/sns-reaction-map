# Image assets

画像は用途ごとに配置する。

- `shared/`: 複数テーマで共有する背景・装飾
- `site/`: トップページなどサイト全体の画像
- `social/`: SNS投稿用画像（テーマ別）
- `topics/<topic>/`: 各テーマ専用のヒーロー、インフォグラフィック、漫画、投票画像

テーマ画像のファイル名は既存の `<topic>-<purpose>.<ext>` 形式を維持する。
生成元の大容量PNGはローカル専用とし、公開用WebPのみを原則として追跡する。
