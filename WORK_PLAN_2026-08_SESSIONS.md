# セッション運用計画 — WORK_PLAN_2026-08 の実行手順

作成: 2026-08-01
親計画: `WORK_PLAN_2026-08.md`
関連: `LOOP.md`（ハブ／ワーカー分担）/ `configs/prompts/`（発注書の置き場）

---

## 0. 結論

**「1工程＝1セッション＝1PR」で正しい。ただし全工程をレビューする必要はない。**

このリポジトリには既にハブ／ワーカーの仕組みがある（LOOP.md ③発注 → ④検証）。
新しい運用を作らず、**それをそのまま使う**。

- **ワーカー = Codex**（リポジトリ内で実装・テスト・PR作成）
- **ハブ = Claude（Cowork）**（発注書の作成と、結果の検証）
- **人間 = 判断と外部作業**（GSC・AdSense・X・ドメイン・マージ）

使用制限を抑える鍵は「レビュー回数を減らす」ことではなく、
**1回のレビューを短くすること**。そのための仕掛けを §3 に置く。

---

## 1. 工程を「レビュー要否」で3段階に分ける

全工程を同じ重さで扱わない。**数字の定義に関わるものだけ厚くレビューする。**

| 段階 | 判定 | 対象 |
|---|---|---|
| **高** | ハブが必ず自分で実行して検証 | 件数の正典、自動検査、両側の論拠 |
| **中** | 成果物のみ確認（複数まとめて1回） | トップ差し替え、ヒーロー統計、レスポンシブ、3分離 |
| **低** | レビュー不要。人間が完了報告のみ | GROWTH.yaml整理、GSC取得、X投稿、AdSense申請、データ更新 |

判定基準はひとつ：**「間違っても誰も気づかない種類の変更か」**。
割れ度が消えたかどうかは目視でわかる。件数の定義が間違っていることは誰も気づかない。だから後者を厚く見る。

---

## 2. セッション割り（全10セッション／4週間）

| # | 週 | 担当 | 内容 | ハブ検証 |
|---|---|---|---|---|
| **S0** | W1 | 人間 | Week 0 の事故防止分を `task/pipeline-safety` でコミット・マージ | 不要 |
| **S1** | W1 | Codex | **件数の正典＋検証ハーネス**（§4 で順序変更） | **必須（高）** |
| **R1** | W1 | ハブ | S1 の検証 | — |
| **S2** | W1 | Codex | トップの問いカード差し替え（A-1） | まとめて |
| **S3** | W1 | Codex | ヒーロー統計＋レスポンシブ（A-2/A-3） | まとめて |
| **R2** | W1末 | ハブ | S2・S3 をまとめて検証（中） | — |
| — | W1 | 人間 | GSC取得・Xフォロワー確認・GROWTH.yaml整理・8/2データ更新 | 不要 |
| **S4** | W2 | Codex | 3分離＋注意書き＋検査ルール追加（A-4/A-5） | **必須（高）** |
| **R3** | W2 | ハブ | S4 の検証 | — |
| — | W2 | 人間 | AdSense 再申請 | 不要 |
| **S5** | W2-3 | Codex | 両側の論拠スキーマ＋代表テーマ1本（B-2） | **必須（高）** |
| **R4** | W3 | ハブ | S5 の検証（内容の質を見る） | — |
| — | W3 | 人間 | X以外の入口を1つ試す・ドメイン調査 | 不要 |
| **R5** | W4 | ハブ | 4週間の総括・横展開の可否判断 | — |

**ハブ（このページ）のセッションは全部で5回。** うち3回は1コマンド実行で終わる想定（§3）。

---

## 3. 使用制限を抑える3つの仕掛け

### 仕掛け1：検証を1コマンドに集約する（最重要）

ハブのセッションが高くつくのは、**毎回リポジトリを探索し直すから**。
探索をやめて、**決まったコマンドを1本叩いて出力を読むだけ**にする。

そのために `scripts/verify_top_page.py` を作る。出力はこの形式に固定する。

```
=== 数値の出所 ===
分類済み投稿   2,946   ← sample_file の実レコード合計（11テーマ）
公開テーマ数      11   ← THEMES.yaml published:done
最終更新    2026-08-02  ← THEMES.yaml updated_at 最大
次回更新    2026-08-09  ← THEMES.yaml refresh_at 最小

=== 置換の空振り検査 ===
OK  hero-total-samples        1件マッチ
OK  公開中のテーマ             1件マッチ
NG  em更新日                   0件マッチ  ← 異常終了

=== 禁止表示 ===
OK  「割れ度」なし
OK  「公開テーマの分類比率」なし
OK  30日以上前の日付なし

=== リンク ===
OK  問いカード 4/4 有効
OK  #ranking 参照なし
```

ハブの検証は「このコマンドを実行して、NG がないことと数値の出所が説明可能なことを確認する」だけになる。
**探索ゼロ、ファイル読み込みゼロ。** これで1セッションのコストが1桁下がる。

### 仕掛け2：Codex の報告フォーマットを固定する

発注書の末尾に必ずこれを入れる。

```markdown
### 完了報告に必ず含めること
1. 変更したファイルの一覧（git diff --stat）
2. `python3 scripts/verify_top_page.py` の出力を**そのまま貼る**
3. 追加・変更した数値それぞれについて「どのファイルの何行目から来たか」
4. 実行したテストコマンドと結果
5. 判断に迷った点（あれば）
```

3 が肝。**数字の出所を書かせる**ことで、ハブは再調査せずに矛盾を検出できる。

### 仕掛け3：中リスクはまとめて1回で見る

S2 と S3 は別PRだが、**レビューは R2 で1回にまとめる**。
低リスク工程（GROWTH.yaml整理、GSC取得、X投稿）は**レビューしない**。人間が完了を報告するだけでよい。

---

## 4. 親計画からの順序変更（1点）

**`WORK_PLAN_2026-08.md` の A-5「自動検査」を Week 2 から Week 1 の S1 に前倒しする。**

理由：

- 検査スクリプトは「最後に品質を確認する道具」ではなく、**以降すべてのレビューを安くする土台**
- S1（件数の正典）で「数字がどこから来るか」を確定させる作業と、それを検証するスクリプトは同じ作業の表裏
- 後ろに置くと、S2・S3 のレビューを手作業でやることになり、ハブのセッションが3回とも高くつく

**分割の仕方**

- **S1**: 検査の**土台**（数値の出所表示・置換の空振り検出）を作る
- **S2以降**: 変更が入るたびに、その PR の中で**検査ルールを1つ追加**する（割れ度の非存在、問いカードのリンク、等）

「テストを最後にまとめて書く」のをやめて、変更と一緒に育てる形にする。

---

## 5. 発注書のテンプレート

`configs/prompts/codex/{YYYYMMDD}_{task}.md` に置く。既存の命名規則に従う。

```markdown
## タスク: {タスク名}

### 出典
WORK_PLAN_2026-08.md の {§番号}

### 目的
{なぜやるか。1〜2文}

### やること
{具体的な手順}

### やらないこと
{スコープ外を明示。「ついでに直す」を防ぐ}

### 制約（必ず守る）
- 保護タグを壊さない: GA4 `G-K10S4YCZFH` / AdSense `ca-pub-2542211932832864` / Supabase / OGP
- ブランチ: `task/{名前}`。main 直接コミット禁止
- `docs/index.html` を `build_site_portal.py` で生成しない
- 数値をハードコードしない。必ず THEMES.yaml か実データファイルから導出する

### 完了条件
- [ ] {検証可能な条件}

### 完了報告に必ず含めること
1. git diff --stat
2. `python3 scripts/verify_top_page.py` の出力をそのまま貼る
3. 追加・変更した数値それぞれの出所（ファイル名と行）
4. 実行したテストコマンドと結果
5. 判断に迷った点
```

---

## 6. ハブのセッションの型

このページで新しいセッションを始めるときは、**この3行だけ**を貼る。

```
WORK_PLAN_2026-08_SESSIONS.md の {S番号} が完了しました。
Codexの完了報告: {報告を貼る}
検証してください。
```

ハブ側の手順は固定：

1. `python3 scripts/verify_top_page.py` を実行
2. NG があれば指摘して終了
3. NG がなければ、報告内の「数値の出所」と出力の整合を確認
4. 高リスク工程のみ、追加で1〜2コマンド実行
5. 合否と、次のセッションの発注書を出す

**やらないこと**: リポジトリ全体の再探索、前回の議論の読み直し、計画全体の再検討。
それが必要になったら、それ自体を独立したセッションにする。

---

## 7. Codex と Claude の使い分け

| | 得意 | この計画での役割 |
|---|---|---|
| **Codex** | リポジトリ内の実装、既存コードへの追従、まとまったコード生成 | S1〜S5 の実装すべて |
| **Claude（ここ）** | 実行しての事実確認、数字の矛盾検出、計画のレビュー | R1〜R5 の検証と発注 |

今回の議論で見つかった3つのバグ（表示値と計算値の2倍乖離／2テーマの count=0／正規表現の空振り）は、
いずれも**コードを読んで**ではなく**実行して**見つかったもの。この分担はそこに合わせている。

逆に、実装そのものをハブでやると探索コストがかかるので、**実装は必ずワーカーに投げる**。

---

## 8. 判断が必要になったときの扱い

セッション中に「これは計画の前提が変わる」という発見があったら、
**その場で計画を作り直さない**。TASK_BOARD.md の連絡メモに1行書いて、次のハブセッションに回す。

| 日付 | 発信AI | 宛先AI | 内容 |
|------|--------|--------|------|

セッションの目的をひとつに保つことが、結果的に使用量を最も減らす。

---

## 9. 今すぐやること

1. **S0**: Week 0 の未コミット分を `task/pipeline-safety` でコミット・PR・マージ（人間・5分）
2. **S1 の発注**: `configs/prompts/codex/20260801_sample-count-canonical.md` を Codex に渡す
3. S1 完了後、このページに §6 の3行を貼る
